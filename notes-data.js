// Lesson notes per unit — written to cover EVERYTHING each unit's quiz/flashcards/exercises test.
// Generated + coverage-verified 2026-07-08. Edit freely; keep the section structure.
window.LESSON_NOTES={
o1:{title:"The Agent Loop",html:"<h3>🎯 What this lesson teaches</h3>\n<p>This lesson teaches <b>the agent loop</b> — how Claude keeps working step by step until a job is done.</p>\n<p>You will learn what makes a chat model into an <b>agent</b>, and the real signal that tells the loop to keep going or stop.</p>\n<p>This is your #1 weak unit. Read it slowly. Every idea here shows up on the quiz.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>The agent loop</b> — an agent does not answer in one shot. It repeats a cycle: <b>think → use a tool → read the result → repeat → finish</b>. It keeps looping until the job is done.<br>🧪 <i>Example:</i> A support agent looks up your order (tool), reads the result, decides a refund is owed, issues it (tool), then stops.<br>⚠️ <i>Trap:</i> Do not think an agent is just \"a bigger prompt\" or \"faster API.\" The loop — acting on results — is what makes it an agent.</div>\n\n<div class=\"idea\"><b>What makes an agent (vs a chat model)</b> — a plain chat model just talks. An agent = a model + <b>tools</b> + a <b>loop</b> that acts on each result.<br>🧪 <i>Example:</i> A chat model can only guess your order status. An agent calls a <code>lookup_order</code> tool, reads the real answer, and acts on it.<br>⚠️ <i>Trap:</i> Using JSON does not make it an agent. JSON is just data. The <b>loop that acts on tool results</b> is the agent part.</div>\n\n<div class=\"idea\"><b>stop_reason runs the loop</b> — after each reply, Claude sends back a field called <b>stop_reason</b>. Your code reads it to decide what to do next. <code>stop_reason == \"tool_use\"</code> means \"keep going.\" <code>stop_reason == \"end_turn\"</code> means \"done.\"<br>🧪 <i>Example:</i> Claude replies with <code>stop_reason: \"tool_use\"</code> → your code runs the tool, sends the result back, and the loop continues.<br>⚠️ <i>Trap:</i> NEVER read the model's words to decide when to stop. Do not scan for \"I'm done.\" Check <b>stop_reason</b> instead — words are unreliable.</div>\n\n<div class=\"idea\"><b>stop_reason == \"tool_use\" is a contract</b> — when Claude says <code>tool_use</code>, it is asking you to run a tool. Your job: run it, put the answer in a <b>tool_result</b>, send it back, then continue the loop.<br>🧪 <i>Example:</i> Claude asks for <code>get_weather(\"Paris\")</code>. You run it, send back <code>tool_result: \"18°C\"</code>, and Claude keeps going with that fact.<br>⚠️ <i>Trap:</i> <code>tool_use</code> does NOT mean \"done\" and does NOT mean \"error.\" It means \"run my tool and feed me the result.\"</div>\n\n<div class=\"idea\"><b>The other stop_reason values</b> — you should recognize the whole family, not just two. Besides <code>tool_use</code> and <code>end_turn</code>, there is <code>max_tokens</code> (ran out of room), <code>stop_sequence</code> (hit a stop word), <code>pause_turn</code> (a long task paused), <code>refusal</code> (safety block), and <code>model_context_window_exceeded</code> (context full).<br>🧪 <i>Example:</i> If a reply gets cut off mid-sentence, you may see <code>stop_reason: \"max_tokens\"</code>.<br>⚠️ <i>Trap:</i> The exam may list an unfamiliar value to scare you. Just remember: <code>tool_use</code> = continue, <code>end_turn</code> = done. The rest each mean a specific \"why it stopped.\"</div>\n\n<div class=\"idea\"><b>Why fixed iteration caps are a WEAK primary stop</b> — some people stop the loop after a fixed number of turns (like \"max 5 turns\"). That is a weak MAIN way to stop. A cap does not understand the task — it can cut off too early (job unfinished) or too late.<br>🧪 <i>Example:</i> A cap of 5 turns kills a task that genuinely needed 6 steps — the user gets a half-done answer.<br>⚠️ <i>Trap:</i> Iteration caps are fine as a SAFETY backstop (a leash so it can't run forever), but NOT as the primary stop. The primary stop is <b>stop_reason</b>.</div>\n\n<div class=\"idea\"><b>The model decides the next tool</b> — in an agent, the model looks at what it sees right now (the context) and picks the next tool itself. It is not following a fixed if-this-then-that decision tree written by you.<br>🧪 <i>Example:</i> After reading an error, the agent decides on its own to try a different tool — you did not hard-code that branch.<br>⚠️ <i>Trap:</i> Don't confuse an agent with a rigid workflow. In a <b>workflow YOU drew the map</b> (fixed steps). In an <b>agent the model holds the map</b> and picks each step live.</div>\n\n<div class=\"idea\"><b>Guardrails so it can't run away</b> — because the loop keeps going on its own, you add safety limits: a max-turn backstop, a sandbox (a safe fenced area), and human checkpoints for risky steps.<br>🧪 <i>Example:</i> An agent may draft a $600 refund, but a guardrail pauses and sends it to a human before it goes through.<br>⚠️ <i>Trap:</i> Guardrails are a leash and a fence — extra safety. They are not the primary stop signal. The primary stop is still <b>stop_reason</b>.</div>\n\n<div class=\"idea\"><b>Report tool failures with is_error</b> — when a tool fails (say a timeout), tell Claude clearly so it can recover. The real API way is a <b>tool_result</b> with <code>is_error: true</code> plus a clear message.<br>🧪 <i>Example:</i> A search times out → send back <code>tool_result</code> with <code>is_error: true</code> and \"Timed out after 5s — try a smaller range.\" Claude then adjusts.<br>⚠️ <i>Trap:</i> There is NO official field called \"isRetryable.\" That is only prep-quiz wording. The real signal is <code>is_error: true</code> + a helpful message. Never crash or return a fake success.</div>\n\n<div class=\"idea\"><b>Start raw, not with a framework</b> — Anthropic's advice: build agents with <b>raw API calls</b> first. A <b>framework</b> (a reusable set of code and rules) makes things easy but hides the prompts, tools, and errors underneath.<br>🧪 <i>Example:</i> A framework may silently retry a failed tool — you can't see or debug it. Raw code shows you everything.<br>⚠️ <i>Trap:</i> \"Glass box vs magic box\" — start with the glass box (you see inside). Add a magic-box framework only once you understand what it hides.</div>\n\n<div class=\"idea\"><b>Don't build an agent when a simple call works</b> — agents cost more money and time. For a well-defined task, one good prompt (plus retrieval and a few examples) is often enough. Save agents for open-ended, unpredictable work.<br>🧪 <i>Example:</i> \"Classify this ticket\" = one prompt. \"Fix bugs across this whole codebase\" = maybe an agent.<br>⚠️ <i>Trap:</i> Agents are a last resort, not a first instinct. Reach for the simplest thing that solves the task.</div>\n\n<div class=\"idea\"><b>The gate pattern (your build exercise)</b> — a <b>chain</b> runs steps in a row: draft → check → route. A <b>gate</b> is a checkpoint BETWEEN two steps. Its rules — not the model — decide pass, revise, or fallback.<br>🧪 <i>Example:</i> Step 1 drafts 3 flashcards. A gate checks them against rules. PASS → done. FAIL → send back to revise. FAIL twice → fallback (keep the best, stop).<br>⚠️ <i>Trap:</i> The PIPELINE'S RULES decide the route, NOT the model. And a real pipeline must never loop forever — always have a fallback stop.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>agent</b> — model + tools + a loop that acts on each result until done.</li>\n<li><b>agent loop</b> — the repeat cycle: think → tool → read result → repeat → finish.</li>\n<li><b>tool</b> — an action you let the model call (a name, description, inputs, and a result you feed back).</li>\n<li><b>tool_use</b> — a stop_reason meaning \"run my tool and return the result, then continue.\"</li>\n<li><b>tool_result</b> — the block you send back carrying a tool's output (or an error).</li>\n<li><b>end_turn</b> — a stop_reason meaning the model is finished.</li>\n<li><b>stop_reason</b> — the field your code checks to run the loop (tool_use = continue, end_turn = done).</li>\n<li><b>is_error</b> — set to true inside a tool_result to report a failure so Claude can recover.</li>\n<li><b>iteration cap</b> — a max-turn limit; a weak PRIMARY stop, fine only as a safety backstop.</li>\n<li><b>guardrail</b> — a safety limit (max turns, sandbox, human checkpoint) so an agent can't run away.</li>\n<li><b>workflow</b> — steps YOU defined in code; you hold the map.</li>\n<li><b>framework</b> — reusable code + rules that ease building but hide prompts, tools, and errors.</li>\n<li><b>gate</b> — a rule-based checkpoint between chain steps that decides pass / revise / fallback.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know the ONE thing the loop checks to decide whether to keep going: it is <b>stop_reason</b> — not word count, not output color, not a filename.</li>\n<li>Know exactly what <code>stop_reason == \"tool_use\"</code> means: run the requested tool, return a tool_result, then continue the loop. It is a contract, not \"done\" and not \"error.\"</li>\n<li>Know that you must NEVER read the model's text to decide when to stop — always check stop_reason.</li>\n<li>Know why a fixed iteration cap is a WEAK primary stop (it doesn't understand task state) and is only okay as a backstop.</li>\n<li>Be able to name what turns a chat model into an agent: tools + a loop that acts on results (JSON alone does not).</li>\n<li>Know the real way to report a tool failure so Claude can recover: a tool_result with <code>is_error: true</code> and a clear message — there is no official \"isRetryable\" field.</li>\n<li>Understand that in an agent the model picks its next tool from context (it holds the map), unlike a fixed workflow where you drew the map.</li>\n<li>Understand the framework caution: start with raw API calls, because a framework hides the prompts, tools, and errors (glass box vs magic box).</li>\n<li>For the build exercise: know a gate is a checkpoint BETWEEN calls, and the pipeline's RULES — not the model — decide pass / revise / fallback, with a fallback so it never loops forever.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Draw the loop in your own words: think → tool → read result → repeat → finish. Add where stop_reason is checked.</li>\n<li>Write the two key values on a card: tool_use = keep going, end_turn = done. List the others (max_tokens, stop_sequence, pause_turn, refusal, model_context_window_exceeded).</li>\n<li>In one line, say why you check stop_reason and NOT the model's words.</li>\n<li>Write why iteration caps are a weak primary stop — and where they DO belong (as a safety backstop).</li>\n<li>Note the tool-failure rule: tool_result + is_error: true + clear message. Add: \"isRetryable is NOT a real field.\"</li>\n</ul>"},
o2:{title:"Orchestrator\u2013Workers",html:"<h3>🎯 What this lesson teaches</h3>\n<p>Big jobs are too much for one agent. So a <b>coordinator</b> splits the work and hands pieces to helper agents.</p>\n<p>The one thing you MUST nail: how this is different from plain parallel work. It is the trap you fell for before.</p>\n<p>You will also learn what to pass a helper, how each helper's loop knows when to stop, and when NOT to use many agents at all.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Orchestrator–Workers</b> — a boss agent (the orchestrator, also called the <b>lead agent</b> or coordinator) breaks a big job into pieces, gives each piece to a helper agent (a <b>worker</b> or <b>subagent</b>), then reads the results and stitches them into one answer.<br>🧪 <i>Example:</i> \"Compare the AI strategy of 5 companies.\" The lead agent makes one worker per company, then merges the 5 reports.<br>⚠️ <i>Trap:</i> the lead agent does NOT search the web itself. It plans, delegates, and synthesizes — it is the editor, not the reporter.</div>\n\n<div class=\"idea\"><b>Orchestrator vs Parallelization (the key difference)</b> — Parallelization = YOUR CODE already knows the subtasks and runs them side by side. Orchestrator = the MODEL decides the subtasks at runtime, freshly, based on the input.<br>🧪 <i>Example:</i> Parallelization = a fixed menu + 3 cooks each making a known dish. Orchestrator = a head chef who invents the dishes after seeing who walked in.<br>⚠️ <i>Trap:</i> \"orchestrator = advanced parallelization\" is WRONG (this was your mistake). The real line: <b>pre-defined subtasks = parallelization; decided-at-runtime subtasks = orchestrator.</b></div>\n\n<div class=\"idea\"><b>Explicit context passing</b> — a subagent starts with a fresh, empty context window. It does NOT know what the lead agent knows. You must put everything it needs into its prompt.<br>🧪 <i>Example:</i> the lead agent must spell out the file paths, the goal, and any decisions already made — the subagent inherits nothing automatically.<br>⚠️ <i>Trap:</i> assuming the worker \"just knows\" the coordinator's info. If you did not pass it, the worker cannot see it.</div>\n\n<div class=\"idea\"><b>Every worker runs an agent loop</b> — a worker is itself an agent. So it repeats one cycle: think, use a tool, read the result, repeat, then finish. The orchestrator runs a loop too, one level up.<br>🧪 <i>Example:</i> a \"research company X\" worker calls a search tool, reads the hits, calls it again, then writes its report and stops.<br>⚠️ <i>Trap:</i> do NOT stop a loop by counting turns (like \"max 5\"). A fixed cap can stop too early or too late because it does not know the task state.</div>\n\n<div class=\"idea\"><b>stop_reason (how the loop decides to keep going)</b> — after each model reply, the loop checks one field: <b>stop_reason</b>. If <code>stop_reason == \"tool_use\"</code>, keep going. If it is <code>end_turn</code>, the agent is done and the loop exits.<br>🧪 <i>Example:</i> worker replies with <code>stop_reason: \"tool_use\"</code> asking for a web search → the loop runs the search. Next reply comes back <code>end_turn</code> → the loop stops and returns the report.<br>⚠️ <i>Trap:</i> the loop checks <b>stop_reason</b> — NOT the output's color, its word count, or a filename. Never read the reply text to guess when to stop; read the field.</div>\n\n<div class=\"idea\"><b>tool_use is a contract</b> — <code>stop_reason == \"tool_use\"</code> means the model is asking you to run a tool. Your job: run the requested tool, put its output in a <b>tool_result</b>, feed that back, then continue the loop.<br>🧪 <i>Example:</i> model asks for <code>get_weather(\"Paris\")</code> → you run it, wrap \"18°C\" in a tool_result, send it back → the model uses it and continues.<br>⚠️ <i>Trap:</i> tool_use does NOT mean \"the model is done\" or \"an error happened\" or \"stop everything.\" It means one thing: run the tool, return a tool_result, keep going. If the tool fails, still return a tool_result but mark it with <code>is_error: true</code> and a clear message.</div>\n\n<div class=\"idea\"><b>When NOT to use multi-agent</b> — use it for jobs that split cleanly into independent parts. Do NOT use it when tasks are <b>tightly coupled</b> — steps that must share live, changing details as they go.<br>🧪 <i>Example:</i> \"research 5 unrelated companies\" splits great (use multi-agent). \"Negotiate a deal where each step depends on the last agreement\" does not (one loop is better).<br>⚠️ <i>Trap:</i> \"too big for one context window\" is a reason FOR multi-agent, not against — each subagent brings its own fresh window.</div>\n\n<div class=\"idea\"><b>Provenance</b> — track where each finding came from. Every result should carry its source, date, and confidence.<br>🧪 <i>Example:</i> a finding = \"refund policy allows 30 days\" + source: refund-policy.md + date: 2026-06-01 + confidence.<br>⚠️ <i>Trap:</i> a finding with no source sounds confident but nobody can check it. No source = can't be verified.</div>\n\n<div class=\"idea\"><b>Match effort to complexity</b> — the coordinator picks only the workers a query actually needs, not the whole pipeline every time.<br>🧪 <i>Example:</i> \"What year was X founded?\" gets 1 agent. \"Compare 5 companies\" gets 10+ subagents.<br>⚠️ <i>Trap:</i> spinning up 10 workers for a one-fact question wastes tokens. Multi-agent uses about 15× the tokens of a plain chat — only worth it for high-value jobs.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>orchestrator / lead agent / coordinator</b> — the boss agent that plans, splits work, and merges results</li>\n<li><b>worker / subagent</b> — a helper agent that does one assigned piece (and runs its own loop)</li>\n<li><b>parallelization</b> — running KNOWN subtasks side by side (your code decides them)</li>\n<li><b>decompose</b> — break a big question into smaller pieces</li>\n<li><b>explicit context passing</b> — putting everything a subagent needs into its prompt</li>\n<li><b>agent loop</b> — the repeat cycle: think → use tool → read result → repeat → finish</li>\n<li><b>stop_reason</b> — the field the loop checks each turn: <code>tool_use</code> = keep going, <code>end_turn</code> = done</li>\n<li><b>tool_use</b> — a stop_reason meaning \"run this tool, return a tool_result, then continue\"</li>\n<li><b>tool_result</b> — the block you feed back with the tool's output (mark <code>is_error: true</code> if it failed)</li>\n<li><b>tightly coupled</b> — steps that depend on each other and must share live info</li>\n<li><b>provenance</b> — the source, date, and confidence tracked for each finding</li>\n<li><b>Task tool</b> — the tool that spawns a subagent (allowedTools must include Task)</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>You must be able to say what makes orchestrator-workers DIFFERENT from parallelization: the orchestrator decides the subtasks dynamically at runtime, while parallelization runs subtasks your code defined ahead of time.</li>\n<li>You must know what an agent loop checks to decide whether to keep going: it checks <b>stop_reason</b> — not output color, word count, or a filename. This is the one field that runs the loop.</li>\n<li>You must know what <code>stop_reason == \"tool_use\"</code> means: run the requested tool and return a <b>tool_result</b>, then continue the loop. It does NOT mean the model is done, that an error occurred, or that you should stop.</li>\n<li>You must know when multi-agent is a POOR fit: tightly-coupled tasks that need shared, live context — because subagents work apart and only report back at the end.</li>\n<li>You must know why you pass a subagent explicit context: it runs in a fresh context window and inherits nothing automatically, so its prompt is the only channel for file paths, errors, and decisions.</li>\n<li>You should recognize the lead-agent role: it plans and delegates and synthesizes, but does not do the searching itself.</li>\n<li>You should know provenance means tracking where each claim came from (source, date, confidence) so results can be trusted and audited.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write, in your OWN words, the one-line difference: parallelization = ______ decides subtasks; orchestrator = ______ decides subtasks.</li>\n<li>Write the one field a loop checks to keep going or stop, and the two values that mean \"keep going\" vs \"done.\"</li>\n<li>Write what you must DO when stop_reason is tool_use (3 steps: run ___, return ___, then ___).</li>\n<li>List the 2 cases: multi-agent GOOD when ______ ; multi-agent BAD when ______.</li>\n<li>Note why a subagent needs explicit context (what does it start with?).</li>\n<li>Write the 3 parts of provenance and why \"no source\" is a problem.</li>\n</ul>"},
rel:{title:"Reliability \u2014 the thread",html:"<h3>🎯 What this lesson teaches</h3>\n<p><b>Reliability</b> is not one stage at the end. It is a habit you use at every level of a build.</p>\n<p>You will learn how to keep the model's memory tight, how to check its work in the right order, how to track where facts came from, and when to hand a case to a human.</p>\n<p>These are the ideas your weak-spot quiz tests. One example each, then how the quiz asks them.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Context window</b> — the model's short-term memory. It only holds so much text at once. Extra text falls out.<br>🧪 <i>Example:</i> you paste a huge document plus a long chat. The oldest lines drop off the \"desk\" and the model can no longer see them.<br>⚠️ <i>Trap:</i> two different problems. <b>Overflow</b> = too much text, so you get errors or the oldest text is dropped. That is NOT the same as lost-in-the-middle (next idea), which is about weak recall in the MIDDLE even when text still fits.</div>\n\n<div class=\"idea\"><b>Lost-in-the-middle</b> — when a context is long, important info buried in the MIDDLE gets overlooked. The model reads the start and end best.<br>🧪 <i>Example:</i> a key date sits halfway down a 40-page paste. The model answers as if it never saw it.<br>⚠️ <i>Trap:</i> the fix is to <b>prune or summarize</b> — cut clutter, pull the key facts up. It is NOT \"the model crashed\" and NOT \"JSON broke.\" Those are quiz distractors.</div>\n\n<div class=\"idea\"><b>Case-facts block</b> — pull the exact amounts, dates, and IDs into their own short block, OUTSIDE the long summary.<br>🧪 <i>Example:</i> a refund case. You keep \"amount: $600, order ID: 4471, date: 2026-06-01\" in a tiny pinned block, so it never gets buried.<br>⚠️ <i>Trap:</i> do not trust that a number inside a long paragraph will survive. Break the critical facts out on their own.</div>\n\n<div class=\"idea\"><b>What survives /compact</b> — when the chat is trimmed, most of the live conversation is lost. Files on disk stay. Your <b>CLAUDE.md</b> and saved <b>memory</b> survive; the mid-chat details do not.<br>🧪 <i>Example:</i> after a compaction the agent forgets a number you said out loud, but still reads the rule written in CLAUDE.md.<br>⚠️ <i>Trap:</i> if a fact matters, write it to a file. Do not leave it only in the chat.</div>\n\n<div class=\"idea\"><b>Scratchpad + crash-recovery manifest</b> — the agent writes progress to files as it goes. If it loses context, it re-reads those files and resumes.<br>🧪 <i>Example:</i> a long job writes \"done: steps 1-3, next: step 4\" to a manifest file. After a reset it opens the file and picks up at step 4.<br>⚠️ <i>Trap:</i> without saved state, a context loss means starting over. The scratchpad is the safety net.</div>\n\n<div class=\"idea\"><b>Verification ladder (code &gt; visual &gt; LLM-judge)</b> — the most robust way to check the agent's work is a RULE or CODE check: tests, types, linters. A person eyeballing it (visual) is weaker. Asking another model to judge (LLM-judge) is the LEAST robust.<br>🧪 <i>Example:</i> to check generated JSON, run a schema test in code — do not just ask the model \"are you sure?\"<br>⚠️ <i>Trap:</i> \"ask the model if it's sure,\" \"trust it,\" and \"longer prompts\" are all weak. Code checks beat them because rules give hard, repeatable feedback.</div>\n\n<div class=\"idea\"><b>Structured error context</b> — when a tool fails, return a clear, structured message: the failure type, what was tried, any partial results, and alternatives. Mark it as an error.<br>🧪 <i>Example:</i> a timeout returns <code>is_error: true</code> with \"timeout after 30s, tried endpoint A, no partial data, try endpoint B.\" Now the agent can recover.<br>⚠️ <i>Trap:</i> the real field is <b>is_error / isError</b> in the tool result. \"isRetryable\" is NOT an official field — treat it only as a name you might invent inside your own message payload, never as a built-in.</div>\n\n<div class=\"idea\"><b>Don't swallow errors, don't over-abort</b> — never return an empty result and call it success. And do not kill the whole workflow because one step failed. Handle the failure with structure and keep the good parts.<br>🧪 <i>Example:</i> 1 of 50 records fails to parse. You report that one as failed and still deliver the other 49 — you do not throw away the batch, and you do not pretend all 50 passed.<br>⚠️ <i>Trap:</i> \"silently return empty as success\" is a classic wrong answer. Silence hides failure.</div>\n\n<div class=\"idea\"><b>Provenance</b> — tracking where each fact or claim came from. A \"receipt\" for every answer.<br>🧪 <i>Example:</i> \"refund policy allows 30 days\" → source: refund-policy.md, dated 2026-06-01. A human can now audit it.<br>⚠️ <i>Trap:</i> provenance is NOT a model's name, a retry policy, or a CSS style. It is source-tracking, and it also settles conflicting or dated data — you keep the newest, sourced version.</div>\n\n<div class=\"idea\"><b>Human-in-the-loop review</b> — a person checks the agent's work before it counts, for cases that are UNCERTAIN, RISKY, or POLICY-SENSITIVE.<br>🧪 <i>Example:</i> the agent approves a $20 refund alone, but a $600 refund goes to a human because policy caps solo approvals at $500.<br>⚠️ <i>Trap:</i> it is NOT there to slow everything down or replace automation. Route only the shaky cases to a person.</div>\n\n<div class=\"idea\"><b>Escalate on complexity, not sentiment</b> — decide to hand off based on how HARD or risky the case is, not on how upset the customer sounds.<br>🧪 <i>Example:</i> a calm customer with a legally tricky claim escalates; an angry customer asking a simple question does not.<br>⚠️ <i>Trap:</i> \"escalate when the customer sounds angry\" is a trap. Feelings are not risk.</div>\n\n<div class=\"idea\"><b>Ask, don't guess, on ambiguity</b> — when input is unclear (e.g. two customers match), ask for another identifier instead of picking one.<br>🧪 <i>Example:</i> two \"J. Smith\" accounts match. The agent asks for an order number rather than guessing.<br>⚠️ <i>Trap:</i> guessing on a match is a silent error waiting to happen.</div>\n\n<div class=\"idea\"><b>Stratified sampling + field-level confidence</b> — humans spot-check a fair slice of EVERY case type (not just easy ones). The agent reports how sure it is per FIELD, so humans look at the shaky fields first.<br>🧪 <i>Example:</i> reviewers check 5% of each category; low-confidence \"address\" fields get checked before high-confidence ones.<br>⚠️ <i>Trap:</i> confidence guides WHERE humans look first — it must never be the ONLY gate deciding IF a human looks at all.</div>\n\n<div class=\"idea\"><b>Batch vs real-time</b> — pick BATCH when results can wait (it is cheaper). Pick REAL-TIME for live or blocking steps.<br>🧪 <i>Example:</i> an overnight report can be batch. A live checkout step or a pre-merge gate must be real-time.<br>⚠️ <i>Trap:</i> a live checkout, an emergency escalation, and a blocking pre-merge gate are all real-time — they are wrong answers for \"when is batch a good fit.\"</div>\n\n<div class=\"idea\"><b>Retries + prompt caching</b> — retry failed calls with a short backoff wait. Cache the stable part of a prompt so repeated calls are cheaper and faster.<br>🧪 <i>Example:</i> a timeout retries after 1s, then 2s. A big system prompt reused all day is cached so you do not pay to resend it each time.<br>⚠️ <i>Trap:</i> retries handle flaky failures; caching cuts cost on repeated context. They are two different tools.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>context window</b> — how much text the model can hold at once</li>\n<li><b>lost-in-the-middle</b> — key info in the middle of a long context gets overlooked</li>\n<li><b>overflow</b> — too much text; you get errors or the oldest text is dropped (different from lost-in-the-middle)</li>\n<li><b>case-facts block</b> — critical amounts/dates/IDs kept in a small pinned block</li>\n<li><b>/compact</b> — trims the chat; files, CLAUDE.md, and memory survive, live chat details do not</li>\n<li><b>scratchpad / manifest</b> — files that let an agent resume after context loss</li>\n<li><b>verification ladder</b> — code &gt; visual &gt; LLM-judge, most to least robust</li>\n<li><b>LLM-judge</b> — using a model to grade output; the least robust check</li>\n<li><b>is_error / isError</b> — the real tool-result flag that marks a failure</li>\n<li><b>provenance</b> — tracking the source of every fact</li>\n<li><b>human-in-the-loop</b> — a person reviews uncertain/risky/policy cases</li>\n<li><b>stratified sampling</b> — spot-check a fair slice of every case type</li>\n<li><b>field-level confidence</b> — the agent's sureness per field</li>\n<li><b>batch</b> — cheaper processing for work that can wait</li>\n<li><b>real-time</b> — for live or blocking steps</li>\n<li><b>retry / backoff</b> — try a failed call again after a short wait</li>\n<li><b>prompt caching</b> — reuse a stable prompt chunk to save cost and time</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Define <b>lost-in-the-middle</b>: important info buried in a long context gets overlooked. Not a crash, not broken JSON, not a tool failure — the fix is prune/summarize.</li>\n<li>Know when <b>batch</b> fits: work that can wait (delayed results OK, cheaper). Reject live checkout, emergency escalation, and blocking pre-merge gates.</li>\n<li>Define <b>provenance</b>: tracking where each claim/fact came from. Not a model name, retry policy, or CSS style.</li>\n<li>Know <b>human-in-the-loop</b> is for routing risky or uncertain (and policy-sensitive) cases to a person — not for slowing things down or replacing automation.</li>\n<li>Name the MOST robust check: rule/code-based checks (tests, types, linters). Above visual, and well above LLM-judge. \"Ask the model,\" \"trust it,\" and \"longer prompts\" are weak.</li>\n<li>Remember the real error flag is <b>is_error / isError</b> with a clear message; \"isRetryable\" is not an official field.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write the verification ladder in your own words: code &gt; visual &gt; LLM-judge, and one reason code wins.</li>\n<li>Draw two \"desks\": one overflowing (overflow) and one where the middle paper is ignored (lost-in-the-middle). Label the difference.</li>\n<li>List the three human-in-the-loop triggers (uncertain, risky, policy-sensitive) and your own $-example.</li>\n<li>Note one line: batch = can wait / cheaper; real-time = live or blocking.</li>\n<li>Jot the provenance \"receipt\" example, and why silence-as-success is the trap to avoid.</li>\n</ul>"},
o3:{title:"An MCP server",html:"<h3>🎯 What this lesson teaches</h3>\n<p><b>MCP</b> is the standard plug that connects Claude to outside systems.</p>\n<p>It has three parts: tools, resources, and prompts. You will learn what each one does.</p>\n<p>You will learn WHO controls each part, and the classic trap: using a tool when a read-only resource fits.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>MCP</b> — a standard \"plug\" that lets Claude connect to outside tools and data. Like a USB port: anything built to the standard fits in.<br>🧪 <i>Example:</i> an MCP server gives Claude a doc catalog to read plus a search_docs action to run.<br>⚠️ <i>Trap:</i> MCP has 3 separate parts. Don't mix them up — tools, resources, and prompts do different jobs.</div>\n\n<div class=\"idea\"><b>Tool</b> — an ACTION you let the model call. It DOES something or changes something.<br>🧪 <i>Example:</i> a create_ticket tool opens a new support ticket. A send_email tool sends mail.<br>⚠️ <i>Trap:</i> if Claude only needs to READ, a tool is the wrong choice — use a resource instead.</div>\n\n<div class=\"idea\"><b>Resource</b> — read-only context that MCP hands to Claude. Claude reads it; it does not change it.<br>🧪 <i>Example:</i> a refund-policy resource lets Claude read the current policy text. A price-list resource is a catalog Claude can look at.<br>⚠️ <i>Trap:</i> the exam loves this — \"a read-only data source for context\" is a RESOURCE, not a tool.</div>\n\n<div class=\"idea\"><b>Prompt (in MCP)</b> — a reusable workflow the user can pick to start a common task.<br>🧪 <i>Example:</i> a \"summarize this PR\" prompt that the user selects from a menu to kick off a set task.<br>⚠️ <i>Trap:</i> in MCP, a \"prompt\" is a saved workflow the user triggers — not just any instruction you type.</div>\n\n<div class=\"idea\"><b>Who controls each part</b> — tools are MODEL-controlled. Resources are APP-controlled. Prompts are USER-controlled.<br>🧪 <i>Example:</i> the model decides to call a tool; the app decides which resources to expose; the user picks a prompt.<br>⚠️ <i>Trap:</i> memorize the pairing. Many wrong answers swap these. Tool = model, resource = app, prompt = user.</div>\n\n<div class=\"idea\"><b>Good tool descriptions</b> — the single biggest factor in how well tools work is a clear, detailed description.<br>🧪 <i>Example:</i> \"Use get_order(id) to look up ONE order by its ID. Returns status and total.\" tells the model what, when, and how.<br>⚠️ <i>Trap:</i> more tools, shorter names, or temperature are NOT the big factor. The DESCRIPTION is. Write it like a docstring for a junior dev.</div>\n\n<div class=\"idea\"><b>Returning a tool error</b> — when a tool fails, send back a tool_result with is_error set to true, plus a clear message. Then Claude can recover.<br>🧪 <i>Example:</i> tool_result { is_error: true, \"Order 55 not found. Ask the user to recheck the ID.\" }<br>⚠️ <i>Trap:</i> do NOT crash, return \"\" , or fake a success. Also: \"isRetryable\" is NOT a real field — that is only a design-your-own-payload idea, not the official mechanism. The real field is is_error (also written isError).</div>\n\n<div class=\"idea\"><b>Consolidate tiny tools</b> — merging several near-identical tools into ONE tool with an \"action\" parameter cuts confusion about which tool to pick.<br>🧪 <i>Example:</i> instead of add_item, remove_item, clear_cart, have one cart(action) tool. Fewer, clearer tools = less wrong-tool guessing.<br>⚠️ <i>Trap:</i> the reason is to reduce tool-choice ambiguity — NOT to use more tokens, skip descriptions, or hide errors.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>MCP</b> — the standard \"plug\" connecting Claude to outside tools and data.</li>\n<li><b>Tool</b> — an action the model can call; it does or changes something.</li>\n<li><b>Resource</b> — read-only context Claude can read but not change.</li>\n<li><b>Prompt (MCP)</b> — a reusable workflow the user picks to start a task.</li>\n<li><b>Model-controlled</b> — the model decides (this is how tools work).</li>\n<li><b>App-controlled</b> — the application decides (this is how resources work).</li>\n<li><b>User-controlled</b> — the user decides (this is how prompts work).</li>\n<li><b>is_error</b> — the flag in a tool_result that tells Claude the tool failed.</li>\n<li><b>Action parameter</b> — one input that lets a single tool do several related jobs.</li>\n<li><b>Tool description</b> — the text that tells the model what a tool is for and when to use it.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know the ONE biggest factor in tool-use performance: clear, detailed tool descriptions — not the number of tools, name length, or temperature.</li>\n<li>Know how to return a tool error so Claude can recover: a tool_result with is_error: true plus a clear message. Not crashing, not empty, not a fake success.</li>\n<li>Know that a read-only data source used for context is a RESOURCE (not a tool, prompt, or transport).</li>\n<li>Know WHO decides when a tool is used: the model. And the full pairing — tools = model-controlled, resources = app-controlled, prompts = user-controlled.</li>\n<li>Know the good reason to merge several tiny tools into one tool with an \"action\" parameter: it reduces tool-choice ambiguity (less wrong-tool confusion).</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write the 3 MCP parts and their one-word jobs: tools = actions, resources = read-only, prompts = workflows.</li>\n<li>Write the control pairing in your own words: tool → model, resource → app, prompt → user.</li>\n<li>Write the read-only rule: if Claude only needs to READ, it is a resource, never a tool.</li>\n<li>Write the tool-error recipe: is_error: true + a clear message so Claude can recover (isRetryable is NOT real).</li>\n<li>Write why one tool with an action parameter beats many tiny tools: less wrong-tool confusion.</li>\n</ul>"},
o4:{title:"Claude Code workflows",html:"<h3>🎯 What this lesson teaches</h3>\n<p>Claude Code is Claude working inside your project on your computer. This lesson shows the three jobs: <b>set it up</b>, <b>run it</b>, and <b>steer it</b>.</p>\n<p>You will learn CLAUDE.md, slash commands, skills, subagents, hooks, and headless mode.</p>\n<p>The one rule above all: a rule that MUST hold goes in a hook, not just a polite prompt line.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>CLAUDE.md</b> — a plain text file of project rules and conventions. Claude re-reads it on every request, so it acts as durable project memory.<br>🧪 <i>Example:</i> a line like \"always run tests before you commit\" lives in CLAUDE.md, so Claude never forgets it.<br>⚠️ <i>Trap:</i> it is NOT for storing API keys, chat history, or a spreadsheet. It holds instructions and conventions.</div>\n\n<div class=\"idea\"><b>CLAUDE.md hierarchy</b> — you can have more than one CLAUDE.md at different levels. A project CLAUDE.md is shared with your team through git. A user-level one is personal to your machine.<br>🧪 <i>Example:</i> team style rules go in the project file; your own \"explain steps to me\" note goes in your user file.<br>⚠️ <i>Trap:</i> the user-level CLAUDE.md is NOT shared through git — teammates never see rules you keep only there.</div>\n\n<div class=\"idea\"><b>Path-specific rules</b> — different conventions for different folders or file types in one project. You do not need a scattered CLAUDE.md in every folder.<br>🧪 <i>Example:</i> one rule file says \"test files (**/*.test.tsx) must use our test format\" and only applies to test files.<br>⚠️ <i>Trap:</i> these rules target files by a glob pattern (like **/*.test.tsx) in the file's frontmatter, so the right rules load for the right area.</div>\n\n<div class=\"idea\"><b>Slash command</b> — a saved prompt you trigger with a slash name. It saves typing the same instruction again.<br>🧪 <i>Example:</i> /review runs your saved \"review this code for bugs\" prompt.<br>⚠️ <i>Trap:</i> a command in the project folder is shared and version-controlled; one in your user folder is personal only.</div>\n\n<div class=\"idea\"><b>Skill (SKILL.md)</b> — a packaged, repeatable workflow: the instructions plus, optionally, a pinned list of allowed tools so it cannot overstep.<br>🧪 <i>Example:</i> a \"write-tests\" skill follows your test format and may only use Read and Write — no Bash, so no risky commands.<br>⚠️ <i>Trap:</i> like commands, a project skill is shared and version-controlled; a user skill is personal.</div>\n\n<div class=\"idea\"><b>Plan mode</b> — Claude writes its plan first and shows you, before touching any files. You approve or edit the plan, THEN the work happens.<br>🧪 <i>Example:</i> \"redesign the login page\" has many valid approaches, so plan first.<br>⚠️ <i>Trap:</i> plan mode is best when the task is complex with more than one good path. Skip it for one tiny obvious fix like a single typo.</div>\n\n<div class=\"idea\"><b>Subagent</b> — a helper Claude that runs in its OWN fresh context window. It does not automatically see the parent's conversation.<br>🧪 <i>Example:</i> an Explore subagent digs through many files, then returns just a short summary — the messy details stay out of the main context.<br>⚠️ <i>Trap:</i> a subagent inherits nothing. The prompt is the only channel — you must pass the file paths, errors, and decisions it needs.</div>\n\n<div class=\"idea\"><b>Hook</b> — a small program that runs automatically at a set moment (like before or after a tool runs). It is a deterministic gate: code decides, not a suggestion.<br>🧪 <i>Example:</i> a PreToolUse hook blocks a refund over the limit every single time, before the tool can run.<br>⚠️ <i>Trap:</i> a rule that must ALWAYS hold belongs in a hook or app-level gate, not a polite prompt line. Prompts can be ignored; hooks cannot.</div>\n\n<div class=\"idea\"><b>Headless mode (claude -p)</b> — run Claude non-interactively from the command line, with no chat. It prints a result you can use in scripts.<br>🧪 <i>Example:</i> a CI pipeline runs claude -p to auto-review each pull request.<br>⚠️ <i>Trap:</i> the -p is for automation and scripts, not for printing images, resetting passwords, or training a model.</div>\n\n<div class=\"idea\"><b>Steering with examples or a failing test</b> — to sharpen Claude's output, give 2 or 3 concrete input/output examples, or write one failing test (TDD) for it to make pass.<br>🧪 <i>Example:</i> instead of \"format the date nicely\", show \"2026-07-08 → Jul 8, 2026\".<br>⚠️ <i>Trap:</i> vague wishes drift; concrete examples and tests pin the target down.</div>\n\n<div class=\"idea\"><b>Interview pattern</b> — let Claude ask you clarifying questions before it builds, so it gets the details right up front.<br>🧪 <i>Example:</i> \"Ask me anything unclear about this feature first, then start.\"<br>⚠️ <i>Trap:</i> skipping this on a fuzzy task means Claude guesses and builds the wrong thing.</div>\n\n<div class=\"idea\"><b>Session control (resume / fork)</b> — --resume continues a past named session where you left off. Fork branches it into a separate copy so you can try something without losing the original.<br>🧪 <i>Example:</i> resume \"refactor-auth\" tomorrow; fork it to test a risky idea safely.<br>⚠️ <i>Trap:</i> after resuming, tell the agent which files changed meanwhile — it does not know what happened while it was away.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>CLAUDE.md</b> — a file of project rules Claude re-reads every request.</li>\n<li><b>hierarchy</b> — project-level vs user-level CLAUDE.md; user-level is not git-shared.</li>\n<li><b>path-specific rule</b> — a rule that applies only to files matching a glob pattern.</li>\n<li><b>slash command</b> — a saved prompt you run by its /name.</li>\n<li><b>skill (SKILL.md)</b> — a packaged workflow, optionally with a pinned allowed-tools list.</li>\n<li><b>plan mode</b> — Claude shows a plan for your approval before editing files.</li>\n<li><b>subagent</b> — a helper with its own fresh context; inherits nothing automatically.</li>\n<li><b>hook</b> — code that runs automatically to enforce a rule deterministically.</li>\n<li><b>deterministic gate</b> — a check enforced by code, so it always holds.</li>\n<li><b>headless / claude -p</b> — non-interactive command-line mode for automation and CI.</li>\n<li><b>--resume / fork</b> — continue a past session, or branch it into a copy.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know that <b>CLAUDE.md holds persistent project instructions and conventions</b>, re-loaded every request — not keys, not chat history, not a spreadsheet.</li>\n<li>Know that <b>claude -p is for non-interactive CLI use in automation</b> (like CI pipelines and scripts).</li>\n<li>Know that <b>plan mode is most useful when the task is complex with multiple valid approaches</b> — not for one obvious word or a trivial fix.</li>\n<li>Know that a <b>subagent does NOT inherit the parent's full conversation</b>; it runs in a fresh context and you must pass needed context in its prompt.</li>\n<li>Know that a <b>rule that must always hold is best enforced by a hook or app-level gate</b> (programmatic), not by a polite prompt line, a bigger model, or more examples.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write, in your own words, what CLAUDE.md is and one thing it must NOT be used for.</li>\n<li>Note the one difference that matters: project CLAUDE.md is git-shared; user-level is not.</li>\n<li>Write the golden rule: safety-critical rules go in a <b>hook</b>, not just a prompt — and say WHY (prompts can be ignored, code cannot).</li>\n<li>Write one line on subagents: fresh context, inherits nothing, so pass context in the prompt.</li>\n<li>Note when to use plan mode (complex, many paths) vs claude -p (automation/CI).</li>\n</ul>"},
m1:{title:"A structured-output call",html:"<h3>🎯 What this lesson teaches</h3>\n<p>How to ask Claude for data in a <b>fixed shape</b> and trust what comes back.</p>\n<p>You send a prompt plus a schema. You get back JSON that fits the shape.</p>\n<p>You also learn the two different \"oops\" moments: broken JSON, and a fact that is simply not there.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Schema</b> — the rules for the shape of your data: which fields exist and what type each one is.<br>🧪 <i>Example:</i> an invoice schema says <code>total</code> is a number and <code>notes</code> is text.<br>⚠️ <i>Trap:</i> a schema is only about shape. It does NOT check if the numbers are true.</div>\n\n<div class=\"idea\"><b>Structured Outputs (constrained decoding)</b> — the system locks the shape WHILE the answer is being written. The model literally cannot break the schema.<br>🧪 <i>Example:</i> schema says age is a number, so you can never get the text \"twenty-five\" back.<br>⚠️ <i>Trap:</i> asking nicely in the prompt, lowering temperature, or retrying 10 times do NOT guarantee the shape. Only constrained decoding does.</div>\n\n<div class=\"idea\"><b>Required vs optional fields</b> — a required field must always be there. An optional field may be left out.<br>🧪 <i>Example:</i> <code>total</code> is required on every invoice; <code>discount</code> is optional.<br>⚠️ <i>Trap:</i> making a field required when real data sometimes lacks it throws GOOD data away. That is \"too strict\".</div>\n\n<div class=\"idea\"><b>Nullable</b> — a field is allowed to say \"not present\" (null) instead of a value.<br>🧪 <i>Example:</i> 5% of invoices have no phone number, so make <code>phone</code> nullable.<br>⚠️ <i>Trap:</i> without nullable, the model may INVENT a fake phone number to fill the box. Nullable stops that hallucination.</div>\n\n<div class=\"idea\"><b>Enum</b> — a field that must be one value from a fixed list of choices.<br>🧪 <i>Example:</i> <code>status</code> can only be \"paid\", \"unpaid\", or \"overdue\".<br>⚠️ <i>Trap:</i> real life has surprises. Add an \"other\" choice plus a free-text detail field so new cases still fit. That keeps the list extensible.</div>\n\n<div class=\"idea\"><b>Invalid JSON vs missing fact</b> — two DIFFERENT problems with two different fixes.<br>🧪 <i>Example:</i> broken JSON (a missing comma) = a FORMAT error → your code retries. A required fact truly absent from the source = a DATA problem → mark it missing or send to a human.<br>⚠️ <i>Trap:</i> never invent a plausible value for a missing fact. Never retry forever hoping a missing fact appears.</div>\n\n<div class=\"idea\"><b>Retry with the specific error</b> — when JSON is malformed, retry and paste the EXACT validation error back in, not just \"try again\".<br>🧪 <i>Example:</i> \"Field <code>total</code> must be a number, got text\" tells the model what to fix.<br>⚠️ <i>Trap:</i> a bare \"try again\" gives the model no clue, so it often makes the same mistake.</div>\n\n<div class=\"idea\"><b>Schema-valid ≠ semantically correct</b> — JSON can fit the shape perfectly and still be WRONG.<br>🧪 <i>Example:</i> <code>total: 999</code> is a valid number but the real invoice said 99. The shape is fine; the fact is wrong.<br>⚠️ <i>Trap:</i> passing the schema check does not mean the data is true. You still need real-world checks or human review.</div>\n\n<div class=\"idea\"><b>Few-shot prompting</b> — showing 2 to 5 examples of the output you want, right in the prompt.<br>🧪 <i>Example:</i> paste two sample invoices with their correct JSON, then ask for the third.<br>⚠️ <i>Trap:</i> \"few-shot\" means FEW EXAMPLES, not fewer words. Do not confuse the two.</div>\n\n<div class=\"idea\"><b>Explicit criteria in a prompt</b> — spelling out clear rules makes the answer sharper and cuts vague or false-positive feedback.<br>🧪 <i>Example:</i> a review prompt that lists exact rules to check gives fewer wrong complaints.<br>⚠️ <i>Trap:</i> vague prompts invite the model to flag things that are actually fine (false positives).</div>\n\n<div class=\"idea\"><b>Prompt chaining</b> — breaking one big task into sequential steps, each step building on the last step's output.<br>🧪 <i>Example:</i> step 1 extracts the raw data, step 2 formats it, step 3 checks it.<br>⚠️ <i>Trap:</i> chaining is NOT one giant prompt, NOT caching, and NOT running in parallel. It trades a little speed for more accuracy.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>schema</b> — rules for the shape of data (fields + types)</li>\n<li><b>JSON</b> — simple text format for structured data, like <code>{ \"a\": 1 }</code></li>\n<li><b>structured outputs</b> — locking the shape during generation so JSON is always valid</li>\n<li><b>constrained decoding</b> — the mechanism that does that locking</li>\n<li><b>required field</b> — must always appear</li>\n<li><b>optional field</b> — may be left out</li>\n<li><b>nullable</b> — allowed to be \"not present\" (null) instead of a value</li>\n<li><b>enum</b> — a value picked from a fixed list of choices</li>\n<li><b>few-shot</b> — giving a few examples of the wanted output</li>\n<li><b>prompt chaining</b> — sequential steps, each using the prior output</li>\n<li><b>false positive</b> — flagging something as wrong when it is actually fine</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know that <b>few-shot prompting</b> means giving EXAMPLES of the behavior you want, not using fewer words.</li>\n<li>Know the most reliable way to get schema-valid JSON is <b>Structured Outputs (constrained decoding)</b> — not asking nicely, not lowering temperature, not retrying many times.</li>\n<li>Know that when a required fact is MISSING from the source you <b>mark it missing or route to a human</b> — you never invent a value, never retry forever, never delete the record.</li>\n<li>Know WHY you add explicit criteria to a review prompt: it <b>reduces vague or false-positive feedback</b>.</li>\n<li>Know that <b>prompt chaining</b> is sequential steps where each step builds on the last output — not one giant prompt, not caching, not parallel runs.</li>\n<li>Be ready for the difference between a FORMAT problem (broken JSON → retry with the error) and a DATA problem (missing fact → human), and that schema-valid still does not mean the fact is correct.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>In your own words: what does a schema promise, and what does it NOT promise?</li>\n<li>Write the one reliable way to force valid JSON, and 3 weak ways that do not work.</li>\n<li>Write the rule for a missing required fact (2 allowed actions, and what is banned).</li>\n<li>Draw the split: broken JSON → do what? vs missing fact → do what?</li>\n<li>Note when you would make a field nullable, and one example where it stops a made-up value.</li>\n</ul>"},
m2:{title:"One tool call",html:"<h3>🎯 What this lesson teaches</h3>\n<p>A <b>tool</b> is one action you let Claude call. You give it a name, a clear description, and inputs.</p>\n<p>Claude uses the tool, then you feed the <b>result</b> back so it can keep going.</p>\n<p>This lesson shows how to write a tool so Claude picks the right one and can recover when things fail.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>A tool = name + description + inputs + result</b> — four parts. The <b>name</b> is a short label. The <b>description</b> tells Claude what the tool does and when to use it. The <b>inputs</b> (also called parameters) are the fields Claude fills in. The <b>result</b> is what you send back after running it.<br>🧪 <i>Example:</i> a <code>lookup_order</code> tool: description \"get the status of one order\", input <code>order_id</code>, result the real order status.<br>⚠️ <i>Trap:</i> a tool is not a normal function. A function serves your code; a tool is a contract a model must understand on its own. Write the description for a model, not for a programmer.</div>\n\n<div class=\"idea\"><b>The description is the #1 thing</b> — good tool-use comes mostly from a clear, detailed description. Not the name length, not the temperature, not how many tools you have.<br>🧪 <i>Example:</i> \"Search customer support tickets by keyword. Returns the 5 newest matches\" beats a bare \"search\".<br>⚠️ <i>Trap:</i> the exam asks for the single biggest factor in tool-use performance. The answer is extremely detailed descriptions — not more tools and not the name.</div>\n\n<div class=\"idea\"><b>Vague descriptions cause wrong tool calls</b> — Claude picks a tool by reading its description. If two tools sound alike, it may pick the wrong one.<br>🧪 <i>Example:</i> <code>analyze_content</code> and <code>analyze_document</code> are near-twins. Claude cannot tell them apart, so it misroutes.<br>⚠️ <i>Trap:</i> do not fix this with a bigger model. Fix it by renaming and giving each tool one clear job.</div>\n\n<div class=\"idea\"><b>tool_result feeds the answer back</b> — after Claude asks for a tool, you run it and return a <b>tool_result</b> block. That is how the loop keeps moving.<br>🧪 <i>Example:</i> Claude asks for <code>lookup_order</code> → you run it → you send back a tool_result saying \"order shipped\". Now Claude can reply.<br>⚠️ <i>Trap:</i> if you never send the result back, Claude is stuck waiting. The result is the only way it learns what happened.</div>\n\n<div class=\"idea\"><b>is_error reports a failure</b> — when a tool fails, return a tool_result with <b>is_error: true</b> and a clear message. Claude reads the message and can recover.<br>🧪 <i>Example:</i> a timeout → return <code>is_error: true</code> with \"Timed out after 5s — try a smaller range\". Claude then tries a smaller range.<br>⚠️ <i>Trap:</i> \"isRetryable\" is NOT a real field — it is only prep-quiz wording. The real signal is <b>is_error + a clear message</b>. You may add your own <code>{category, retryable}</code> inside your payload so your code can route retries, but that is your design, not an official API field.</div>\n\n<div class=\"idea\"><b>Empty result is not a failure</b> — an empty list <code>[]</code> means \"the tool ran fine and found nothing\". A failure (like a timeout) means \"the tool could not run\". Handle them differently.<br>🧪 <i>Example:</i> search returns <code>[]</code> → tell Claude \"no matches\" (success). A timeout → return is_error so Claude retries.<br>⚠️ <i>Trap:</i> do not mark an empty result as an error. Nothing found is a valid answer.</div>\n\n<div class=\"idea\"><b>tool_choice: auto / any / forced</b> — this setting tells Claude whether it must use a tool. <b>auto</b> = Claude decides. <b>any</b> = it must use some tool. <b>forced</b> = it must use one specific tool you name.<br>🧪 <i>Example:</i> in a form-filler you force the <code>submit_form</code> tool so Claude cannot just chat.<br>⚠️ <i>Trap:</i> auto lets Claude also answer in plain text. If you need a tool every time, use any or forced.</div>\n\n<div class=\"idea\"><b>Split vs merge tools</b> — if tools do DIFFERENT jobs but the names blur, <b>split and rename</b> them. If tools do the SAME job in small variations, <b>merge</b> them into one tool with an <b>action</b> field.<br>🧪 <i>Example:</i> <code>add_task</code> / <code>update_task</code> / <code>delete_task</code> confuse the picker. Merge into one <code>manage_task</code> with action = \"create\" / \"update\" / \"delete\".<br>⚠️ <i>Trap:</i> merging reduces tool-choice ambiguity. The reason is fewer, clearer choices — not to save tokens or hide errors.</div>\n\n<div class=\"idea\"><b>Keep tools few and on-role</b> — aim for about 4 to 5 tools per agent, not 18. Give each agent only the tools that fit its job.<br>🧪 <i>Example:</i> a support agent gets <code>lookup_order</code> and <code>issue_refund</code> — not database-admin tools.<br>⚠️ <i>Trap:</i> off-role tools get misused. Too many tools also blur the choice and cause wrong picks.</div>\n\n<div class=\"idea\"><b>MCP tool vs resource vs prompt</b> — MCP is a standard plug for connecting Claude to outside systems. It has three parts. A <b>tool</b> is an action (model-controlled). A <b>resource</b> is read-only context (app-controlled). A <b>prompt</b> is a saved workflow (user-controlled).<br>🧪 <i>Example:</i> a refund-policy document is a resource (you read it). Issuing a refund is a tool (you act).<br>⚠️ <i>Trap:</i> a read-only data source for context is a resource, NOT a tool. And the model decides when a tool is used — not the user or the admin.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>tool</b> — one action you let the model call (name, description, inputs, result).</li>\n<li><b>description</b> — the text that tells Claude what a tool does and when to use it; the biggest factor in getting the right call.</li>\n<li><b>inputs / parameters</b> — the fields Claude fills in to run a tool.</li>\n<li><b>tool_result</b> — the block you send back after running a tool, so the loop continues.</li>\n<li><b>is_error</b> — a flag on a tool_result meaning the tool failed; pair it with a clear message.</li>\n<li><b>isRetryable</b> — NOT a real API field; only prep-quiz wording. Real signal = is_error + message.</li>\n<li><b>empty result []</b> — a successful run that found nothing; different from a failure.</li>\n<li><b>tool_choice</b> — setting for whether Claude must use a tool: auto, any, or forced.</li>\n<li><b>action field</b> — one input like \"create\" / \"update\" / \"delete\" that merges near-twin tools into one.</li>\n<li><b>MCP resource</b> — read-only context (docs, catalogs) exposed over MCP; app-controlled.</li>\n<li><b>MCP prompt</b> — a saved workflow exposed over MCP; user-controlled.</li>\n<li><b>namespacing</b> — clear prefixes like <code>asana_search</code> vs <code>jira_search</code> so the right tool gets picked among many.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know the single biggest factor in tool-use performance: extremely detailed descriptions — not name length, temperature, or tool count.</li>\n<li>Know how to return a tool error so Claude can recover: a tool_result with is_error: true plus a clear message. Remember \"isRetryable\" is a trap word.</li>\n<li>Know that in MCP a read-only data source for context is a resource (tools = actions, prompts = workflows).</li>\n<li>Know who decides when a tool is used in MCP: the model. (Prompts are user-controlled; resources are app-controlled.)</li>\n<li>Know the good reason to merge tiny tools into one with an action parameter: to reduce tool-choice ambiguity, so Claude picks the right thing.</li>\n<li>Understand why near-identical descriptions (analyze_content vs analyze_document) cause misrouting, and that the fix is rename/split into purpose-built tools.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write the four parts of a tool in your own words, with one tiny example of each.</li>\n<li>Write one sentence on why the description matters most, and what a vague one causes.</li>\n<li>Write the correct way to report a tool failure, and cross out the fake word \"isRetryable\".</li>\n<li>Write the difference between an empty result [] and a real failure.</li>\n<li>Write the three MCP parts (tool / resource / prompt) and who controls each.</li>\n</ul>"},
m4:{title:"Built-in tools",html:"<h3>🎯 What this lesson teaches</h3>\n<p>Claude Code comes with a small set of <b>built-in tools</b>. This lesson is about picking the right one.</p>\n<p>You will learn what Grep, Glob, Read, Write, Edit, and Bash each do. And one smart move: grep an entry point, then read to follow the code.</p>\n<p>You will also learn the rules for tools in general — good descriptions, how to return errors, and who decides when a tool runs.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Grep</b> — searches <i>inside</i> files. It looks at the text (the content) and finds lines that match.<br>🧪 <i>Example:</i> \"Find every file that mentions <code>login</code>.\" Grep reads inside files and shows you the hits.<br>⚠️ <i>Trap:</i> Grep does NOT match filenames. If you want files <i>named</i> a certain way, that is Glob, not Grep.</div>\n\n<div class=\"idea\"><b>Glob</b> — matches file <i>names</i> and paths by a pattern. It does not look inside files at all.<br>🧪 <i>Example:</i> <code>**/*.test.ts</code> finds every test file, in any folder, by its name.<br>⚠️ <i>Trap:</i> Glob only knows names and paths. It cannot find a word that lives <i>inside</i> a file — that job is Grep.</div>\n\n<div class=\"idea\"><b>Read / Write</b> — Read opens a whole file so Claude can see it. Write saves a whole file (creates it or replaces it).<br>🧪 <i>Example:</i> Read <code>server.js</code> to see all its code. Write a brand-new <code>notes.txt</code> from scratch.<br>⚠️ <i>Trap:</i> Write replaces the <i>entire</i> file. To change just one line, do not Write the whole thing — use Edit.</div>\n\n<div class=\"idea\"><b>Edit</b> — changes <i>one unique piece of text</i> inside a file, leaving the rest alone.<br>🧪 <i>Example:</i> change <code>timeout = 30</code> to <code>timeout = 60</code> in a config file.<br>⚠️ <i>Trap:</i> Edit needs the text to be <b>unique</b>. If the same text appears many times, Edit can't tell which one — then Read the file and Write it back with your change.</div>\n\n<div class=\"idea\"><b>Bash</b> — runs a shell command, like in a terminal. Use it to run programs, tests, or git.<br>🧪 <i>Example:</i> <code>npm test</code> to run tests, or <code>git status</code> to see changes.<br>⚠️ <i>Trap:</i> Bash is for running commands. Don't use Bash to search or read files when Grep, Glob, or Read already do that cleanly.</div>\n\n<div class=\"idea\"><b>Grep an entry point, then Read to follow imports</b> — the smart way to explore a big codebase you don't know.<br>🧪 <i>Example:</i> Grep for <code>main</code> or <code>app.listen</code> to find where the program starts. Then Read that file, see what it imports, and Read those files next.<br>⚠️ <i>Trap:</i> Don't Read every file blindly. Start with one search to find the door, then follow the trail of imports.</div>\n\n<div class=\"idea\"><b>A tool's description is the biggest factor in tool use</b> — a clear description tells the model what the tool does, when to use it, and how.<br>🧪 <i>Example:</i> \"search_orders — look up a customer's past orders by their email\" beats a vague name with no words.<br>⚠️ <i>Trap:</i> The distractors are \"shorter names\", \"temperature\", or \"more tools\". None of those. Clear, detailed <i>descriptions</i> win.</div>\n\n<div class=\"idea\"><b>Return a tool error with is_error</b> — when a tool fails, send back a <code>tool_result</code> with <code>is_error: true</code> plus a clear message. Then the model can recover.<br>🧪 <i>Example:</i> a lookup fails → return <code>is_error: true</code>, \"Order not found, ask the user to recheck the ID.\"<br>⚠️ <i>Trap:</i> \"isRetryable\" is NOT a real field — it's only a design-your-own-payload idea. The official mechanism is <code>is_error</code> (also written <code>isError</code>). Never crash, and never fake a success.</div>\n\n<div class=\"idea\"><b>The model decides when a tool runs</b> — tools are model-controlled. The model reads the context and chooses.<br>🧪 <i>Example:</i> the model sees \"refund this order\", so it decides to call the refund tool.<br>⚠️ <i>Trap:</i> Don't confuse the three MCP parts. Tools = model-controlled. Prompts = user-controlled. Resources = app-controlled and read-only.</div>\n\n<div class=\"idea\"><b>Resource = read-only context</b> — if the model only needs to READ something, expose it as a resource, not a tool.<br>🧪 <i>Example:</i> a refund-policy document the model reads = a resource. Actually issuing a refund = a tool (an action).<br>⚠️ <i>Trap:</i> The classic mistake is making a tool when a read-only resource fits. Read = resource. Act = tool.</div>\n\n<div class=\"idea\"><b>Merge tiny tools into one with an action parameter</b> — too many near-identical tools confuse the model. One tool with an <code>action</code> field is clearer.<br>🧪 <i>Example:</i> instead of add_item, remove_item, list_items, make one <code>cart</code> tool with <code>action: \"add\" | \"remove\" | \"list\"</code>.<br>⚠️ <i>Trap:</i> The reason is to <i>reduce wrong-tool confusion</i> — not to save tokens or skip descriptions.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>Grep</b> — search inside files for matching text (content).</li>\n<li><b>Glob</b> — match filenames and paths by a pattern (like <code>**/*.test.ts</code>).</li>\n<li><b>Read</b> — open a whole file to see it.</li>\n<li><b>Write</b> — save a whole file (create or fully replace it).</li>\n<li><b>Edit</b> — change one unique piece of text inside a file.</li>\n<li><b>Bash</b> — run a shell command (tests, git, programs).</li>\n<li><b>Entry point</b> — where a program starts running (like <code>main</code>).</li>\n<li><b>Tool</b> — an action you let the model call.</li>\n<li><b>Resource (MCP)</b> — read-only context the model can read, not change.</li>\n<li><b>is_error</b> — the flag in a tool_result that says the tool failed.</li>\n<li><b>Action parameter</b> — one field that picks a mode, so several tools become one.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know that <b>Grep searches inside files (content)</b> and <b>Glob matches filenames/paths</b> — pick the right one for \"find a word\" vs \"find files named X\".</li>\n<li>Know that <b>Write replaces a whole file</b> and <b>Edit changes one unique piece of text</b> — and if the text isn't unique, fall back to Read then Write.</li>\n<li>Know that <b>Bash runs commands</b>, not searches or reads.</li>\n<li>Know the explore pattern: <b>Grep an entry point, then Read to follow imports</b> — don't read everything blindly.</li>\n<li>Know the <b>single biggest factor in tool use is a detailed description</b> — not name length, temperature, or number of tools.</li>\n<li>Know how to <b>return a tool error</b>: a <code>tool_result</code> with <code>is_error: true</code> and a clear message. Remember \"isRetryable\" is NOT official.</li>\n<li>Know <b>who controls each MCP part</b>: tools = model-controlled, prompts = user-controlled, resources = app-controlled (read-only).</li>\n<li>Know that a <b>read-only data source is a resource</b>, and using a tool where a resource fits is the trap.</li>\n<li>Know the reason to <b>merge tiny tools into one with an action parameter</b>: reduce wrong-tool confusion.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write your own one-line rule for Grep vs Glob (inside files vs filenames).</li>\n<li>Write when you'd pick Edit and when you'd fall back to Read + Write.</li>\n<li>Draw the explore pattern: Grep entry point → Read → follow imports → Read next.</li>\n<li>Write the three MCP controllers in your own words: tool = model, prompt = user, resource = app.</li>\n<li>Write the correct way to return a tool error, and note that \"isRetryable\" is a fake distractor.</li>\n</ul>"},
m3:{title:"Prompt techniques",html:"<h3>🎯 What this lesson teaches</h3>\n<p>How to write prompts that make a system <b>reliable</b>, not just clever.</p>\n<p>You learn four moves: explicit criteria, few-shot examples, output format, and prompt chaining with gates.</p>\n<p>The big shift: stop trying to make one answer good. Start making the whole system predictable.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Explicit criteria</b> — write the exact rules you want, not vague wishes. Clear rules cut down vague and false-positive feedback.<br>🧪 <i>Example:</i> Instead of \"be conservative\", say \"only flag a bug if it changes program output.\" A code review with clear rules flags fewer things that were actually fine.<br>⚠️ <i>Trap:</i> \"be conservative\" or \"use good judgment\" sounds careful but is too vague. It will NOT reduce false positives. The exam wants \"clear criteria = fewer false positives.\"</div>\n\n<div class=\"idea\"><b>Few-shot prompting</b> — you show the model a few EXAMPLES of the answer you want, then ask the real question. \"Few\" means few examples, NOT fewer words.<br>🧪 <i>Example:</i> \"Ticket: card charged twice → billing. Ticket: app crashes on login → bug. Ticket: how do I reset my password? → ___\". The examples teach the pattern, so the model copies it.<br>⚠️ <i>Trap:</i> Few-shot does NOT mean \"using fewer words\" or \"one-word prompts\" or \"removing instructions.\" It means giving 2–5 examples of the behavior you want.</div>\n\n<div class=\"idea\"><b>Few-shot teaches reasoning</b> — good examples teach the WHY, so the model handles new cases it never saw. Use 2–4 targeted examples for the tricky, ambiguous cases.<br>🧪 <i>Example:</i> Show one \"yes\" and one \"no\" example near the fuzzy boundary. Now the model can judge a brand-new borderline case correctly.<br>⚠️ <i>Trap:</i> One good + one bad example is a start, but ambiguous tasks need a few targeted examples, not just one of each.</div>\n\n<div class=\"idea\"><b>Output format</b> — tell the model the exact shape you want back. This makes the answer easy for code to use.<br>🧪 <i>Example:</i> \"Extract the invoice total as JSON. If missing, return null.\" Now every answer looks the same and your code can read it.<br>⚠️ <i>Trap:</i> Asking nicely in the prompt does not GUARANTEE the shape (see the next idea).</div>\n\n<div class=\"idea\"><b>Structured Outputs</b> — the most reliable way to get valid JSON. It uses <b>constrained decoding</b>: the model is forced to produce output that fits your schema. The shape is guaranteed, not left to luck.<br>🧪 <i>Example:</i> You define a schema {total: number}. Structured Outputs makes sure the reply always fits it — no missing braces.<br>⚠️ <i>Trap:</i> \"Ask nicely\", \"lower temperature only\", or \"retry 10 times\" do NOT guarantee valid JSON. Only constrained decoding guarantees the shape.</div>\n\n<div class=\"idea\"><b>Missing facts — never fabricate</b> — if a required fact is not in the source, the system should mark it missing or send it to a human. It must never invent a value.<br>🧪 <i>Example:</i> The invoice has no total. Return null or \"missing\" and route to review — do not guess \"$100.\"<br>⚠️ <i>Trap:</i> \"Invent a plausible value\", \"retry forever\", or \"delete the whole record\" are all wrong. Flag it missing or hand it to a person.</div>\n\n<div class=\"idea\"><b>Prompt chaining</b> — break one big task into sequential steps. Each step uses the previous step's output. Each call gets one easy job.<br>🧪 <i>Example:</i> Step 1 write an outline → Step 2 write the doc from that outline. Or: copy → translate.<br>⚠️ <i>Trap:</i> Chaining is NOT one giant prompt, NOT caching, NOT running prompts in parallel. It is sequential steps, each building on the last output. It trades a bit of latency for more accuracy.</div>\n\n<div class=\"idea\"><b>Gate</b> — a programmatic CHECK placed between two steps of a chain. Output 1 only moves on if it passes the rules. If it fails, you revise or use a fallback. <b>Your code decides, not the model.</b><br>🧪 <i>Example:</i> Outline → GATE checks it has 3 sections → only then write the doc. Think of a relay race with a referee checking each handoff.<br>⚠️ <i>Trap:</i> The exam loves asking WHO decides the next step. Answer: the pipeline's rules (your code), not the model's vibes.</div>\n\n<div class=\"idea\"><b>Gate the middle steps, not just the end</b> — checking early catches errors before they pile up and get expensive to trace.<br>🧪 <i>Example:</i> If a bad outline slips through, the whole doc is wrong. Catching it at the gate saves the whole downstream chain.<br>⚠️ <i>Trap:</i> Only checking the final answer means an early mistake has already spread everywhere and is hard to find.</div>\n\n<div class=\"idea\"><b>Fix the noisy category, do not tolerate it</b> — if one label or check is unreliable, it erodes trust in ALL of them. Disable it and fix the prompt.<br>🧪 <i>Example:</i> A \"spam\" label is wrong half the time. Turn it off and repair the prompt before turning it back on.<br>⚠️ <i>Trap:</i> Leaving one noisy category running makes people doubt the good ones too.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>Prompt</b> — the instruction you give the model; it shapes behavior, output format, and error handling.</li>\n<li><b>Explicit criteria</b> — the exact rules written into the prompt, replacing vague guidance.</li>\n<li><b>Few-shot</b> — showing 2–5 examples of the wanted output before the real question.</li>\n<li><b>Output format</b> — the shape you ask the answer to come back in (e.g. JSON).</li>\n<li><b>Structured Outputs</b> — a feature that guarantees valid JSON via constrained decoding.</li>\n<li><b>Constrained decoding</b> — forcing the model's output to fit a schema so it is always valid.</li>\n<li><b>Prompt chaining</b> — splitting a task into sequential steps, each using the last output.</li>\n<li><b>Gate</b> — a programmatic check between steps; code decides pass, revise, or fallback.</li>\n<li><b>Fallback</b> — the safe backup path when a step keeps failing, so the chain never loops forever.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>You must know few-shot means giving EXAMPLES of the behavior you want — not fewer words, not one-word prompts.</li>\n<li>You must know the most reliable way to get schema-valid JSON is Structured Outputs (constrained decoding) — not asking nicely, lowering temperature, or retrying.</li>\n<li>You must know that when a required fact is missing, the system marks it missing or routes to a human — it never fabricates, retries forever, or deletes the record.</li>\n<li>You must know explicit criteria are added to reduce vague or false-positive feedback — clear criteria mean fewer false positives.</li>\n<li>You must know prompt chaining is sequential steps, each building on the last output — not one giant prompt, not caching, not parallel runs. It trades latency for accuracy.</li>\n<li>From the exercise and flashcards: you must know a GATE is a programmatic check BETWEEN calls, and the PIPELINE'S RULES (your code) decide pass / revise / fallback — not the model. Gate middle steps to catch errors early.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write \"few-shot = a few EXAMPLES, not fewer words\" plus your own 2-line example prompt.</li>\n<li>Write the one reliable way to get valid JSON, and why \"ask nicely / retry / lower temperature\" fail.</li>\n<li>Write what to do when a required fact is missing (two options), and the one thing you must never do.</li>\n<li>Write the chaining definition in your own words: sequential steps, each uses the last output, trades latency for accuracy.</li>\n<li>Draw the draft → gate → route pipeline and write, in your own words, WHO decides the route.</li>\n</ul>"},
t1:{title:"Support-agent system",html:"<h3>🎯 What this lesson teaches</h3>\n<p>You will design a real <b>support-agent system</b>. It checks an order, decides a refund, and passes hard cases to a human.</p>\n<p>The big rule: a limit that MUST hold (like \"no big refunds\") lives in <b>code or a hook</b>, never just in the prompt.</p>\n<p>You will also learn the shape of a good handoff to a person.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>The agent loop</b> — an agent is a model plus tools plus a LOOP: think → use a tool → read the result → repeat → finish. The support agent loops until the case is solved.<br>🧪 <i>Example:</i> customer asks for a refund. Loop: call <code>get_order</code> → read it → call <code>get_customer</code> → decide → answer.<br>⚠️ <i>Trap:</i> a loop with no stop rule can run forever. It needs a clear way to STOP.</div>\n\n<div class=\"idea\"><b>stop_reason — the loop's traffic light</b> — after each model turn you read a field called <b>stop_reason</b>. That tells the loop whether to keep going or quit.<br>🧪 <i>Example:</i> the code says \"while stop_reason == 'tool_use', run the tool and continue; otherwise stop.\"<br>⚠️ <i>Trap:</i> do NOT stop the loop just by counting turns (a fixed cap). A cap can stop too early or too late. Read stop_reason instead — it reflects the real task state.</div>\n\n<div class=\"idea\"><b>stop_reason == 'tool_use' is a contract</b> — when the model says <b>tool_use</b>, it is asking you to run a tool. Your job: run it, put the answer in a <b>tool_result</b>, and feed that back. Then the loop continues.<br>🧪 <i>Example:</i> model returns tool_use for <code>get_order(123)</code>. You run it, send back the order data, the model reads it and keeps going.<br>⚠️ <i>Trap:</i> tool_use does NOT mean \"done\" and does NOT mean \"error\". It means \"run this tool and give me the result\".</div>\n\n<div class=\"idea\"><b>A hard rule goes in a HOOK, not a prompt</b> — a <b>hook</b> is a deterministic gate in code that runs before or after a tool. \"Deterministic\" means it fires the same way every time. Use it for any rule that must ALWAYS hold.<br>🧪 <i>Example:</i> a PreToolUse hook blocks <code>process_refund</code> whenever the amount is over $100, so a big refund is escalated to a human — every time.<br>⚠️ <i>Trap:</i> \"Please don't refund over $100\" in the prompt is only a request. The model can slip. Safety-critical rules must be code/hooks, not polite words.</div>\n\n<div class=\"idea\"><b>Prerequisite hook</b> — a hook can also block a tool until another step has happened first. This enforces order.<br>🧪 <i>Example:</i> block <code>process_refund</code> until <code>get_customer</code> has returned a verified ID. No verified ID → no refund runs.<br>⚠️ <i>Trap:</i> don't trust the model to \"remember\" to verify first. The hook makes the order guaranteed.</div>\n\n<div class=\"idea\"><b>Escalate on request</b> — if the customer clearly asks for a human, hand off right away. Don't argue or loop.<br>🧪 <i>Example:</i> \"I want to speak to a manager\" → the agent stops trying and routes to a person.<br>⚠️ <i>Trap:</i> forcing the agent to keep solving after a clear human request is the wrong move.</div>\n\n<div class=\"idea\"><b>Split a multi-issue request</b> — if one message has several separate problems, split it, look into each part (can be in parallel), then merge into ONE reply.<br>🧪 <i>Example:</i> \"my order is late AND I was double-charged\" → investigate both → send one clear resolution covering both.<br>⚠️ <i>Trap:</i> only works when the parts are independent. Tightly-linked parts that need shared context should not be split apart.</div>\n\n<div class=\"idea\"><b>Structured handoff fields</b> — when you pass a case to a human, send fixed fields, not a vague story. A good <b>handoff</b> = customer ID + root-cause analysis + recommended action.<br>🧪 <i>Example:</i> \"Customer #42 · root cause: payment ran twice · recommend: refund one charge, apologize.\"<br>⚠️ <i>Trap:</i> a handoff with no clear fields makes the human redo all the work. Structure saves time.</div>\n\n<div class=\"idea\"><b>Provenance in the handoff</b> — <b>provenance</b> means tracking where each fact came from. In a handoff, tie claims to their source so a human can check them.<br>🧪 <i>Example:</i> \"refund allowed = 30 days (source: refund-policy.md, 2026-06-01).\"<br>⚠️ <i>Trap:</i> a confident claim with no source can't be verified. No source = no trust.</div>\n\n<div class=\"idea\"><b>PostToolUse hook to clean data</b> — a hook can also tidy a tool's output BEFORE the model reads it. This is a <b>PostToolUse</b> hook.<br>🧪 <i>Example:</i> one system returns dates as \"07/06\", another as a number. A PostToolUse hook normalizes them to one format first.<br>⚠️ <i>Trap:</i> feeding mixed, messy formats straight to the model causes confusion. Normalize first.</div>\n\n<div class=\"idea\"><b>Return tool errors so the agent can recover</b> — when a tool fails, don't crash. Return a <b>tool_result</b> marked <code>is_error: true</code> with a clear message. The model can then try to fix things.<br>🧪 <i>Example:</i> order lookup times out → return is_error true, \"lookup timed out, retry\". The agent retries or escalates.<br>⚠️ <i>Trap:</i> \"isRetryable\" is NOT a real official field. The real mechanism is <code>is_error</code> (isError). You may design your own extra fields, but is_error is the standard one.</div>\n\n<div class=\"idea\"><b>Workflow vs agent</b> — in a <b>workflow</b>, YOUR code decided every step ahead of time. In an <b>agent</b>, the model decides its own next step, turn by turn.<br>🧪 <i>Example:</i> a fixed \"draft → gate → route\" pipeline is a workflow. A support bot that chooses which tool to call next is an agent.<br>⚠️ <i>Trap:</i> don't call a fixed code path an \"agent\". The agent is the one holding the map.</div>\n\n<div class=\"idea\"><b>Orchestrator-workers vs parallelization</b> — both run several jobs. The difference is WHO picks the jobs. In <b>parallelization</b> your code picks the subtasks ahead of time. In <b>orchestrator-workers</b> the lead model decides the subtasks at runtime, per input.<br>🧪 <i>Example:</i> splitting \"late + double-charge\" into two known checks = parallelization. A lead model deciding on the spot how to break up a fuzzy request = orchestrator-workers.<br>⚠️ <i>Trap:</i> orchestrator-workers is NOT just \"advanced parallelization\". The key word is dynamic — subtasks are decided at runtime.</div>\n\n<div class=\"idea\"><b>Subagents inherit nothing</b> — a subagent runs in a fresh context window. It does NOT get the parent's chat automatically. You must pass what it needs in its prompt.<br>🧪 <i>Example:</i> hand a worker the customer ID, the error message, and the file path — spell it out, don't assume it \"knows\".<br>⚠️ <i>Trap:</i> multi-agent is a POOR fit for tightly-coupled tasks that need shared context mid-flight — subagents can't coordinate live.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>agent</b> — model + tools + a loop that keeps acting until the job is done</li>\n<li><b>agent loop</b> — think → use tool → read result → repeat → finish</li>\n<li><b>stop_reason</b> — the field the loop reads to decide keep-going or quit</li>\n<li><b>tool_use</b> — a stop_reason meaning \"run this tool and give me the result\"</li>\n<li><b>tool_result</b> — the message that carries a tool's output (or error) back to the model</li>\n<li><b>is_error</b> — the real field that marks a tool_result as a failure so the agent can recover</li>\n<li><b>hook</b> — a deterministic code gate that runs before (PreToolUse) or after (PostToolUse) a tool</li>\n<li><b>deterministic</b> — behaves the same way every time, no luck involved</li>\n<li><b>escalate</b> — hand the case to a human</li>\n<li><b>handoff</b> — the package sent to a human: customer ID + root cause + recommended action</li>\n<li><b>provenance</b> — tracking the source of each fact or claim</li>\n<li><b>workflow</b> — steps your code decided ahead of time</li>\n<li><b>orchestrator-workers</b> — a lead model decides subtasks at runtime and delegates them</li>\n<li><b>parallelization</b> — pre-planned subtasks run at the same time</li>\n<li><b>subagent</b> — a helper model with a fresh context; gets only what you pass it</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know what the loop reads each turn to decide whether to continue: it checks <b>stop_reason</b>, not word count or color or filename.</li>\n<li>Know what <b>stop_reason == 'tool_use'</b> means: it is a contract — run the requested tool, return a tool_result, then continue. It does not mean \"done\" or \"error\".</li>\n<li>Be able to say why <b>orchestrator-workers</b> differs from parallelization: the lead model decides the subtasks dynamically at runtime, instead of using pre-defined ones.</li>\n<li>Know when multi-agent is a POOR fit: <b>tightly-coupled tasks that need shared context</b> — subagents can't coordinate mid-flight.</li>\n<li>Know why you pass a subagent explicit context: it runs in a fresh context and inherits nothing, so file paths, errors, and decisions must go in its prompt.</li>\n<li>Know that a rule that must ALWAYS hold (block a big refund) is enforced by a <b>hook or app-level gate</b>, not a polite prompt line — because safety-critical means deterministic.</li>\n<li>Know the honest error mechanism: return a tool_result with <b>is_error: true</b> and a clear message; \"isRetryable\" is not a real official field.</li>\n<li>Know why a fixed iteration cap is a weak PRIMARY stop method: it can't read the real task state — check stop_reason instead.</li>\n<li>Know what a <b>handoff</b> should carry: customer ID + root-cause analysis + recommended action, with provenance so a human can audit it.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Draw the loop: think → tool → read result → repeat → stop. Label the field the loop reads (stop_reason).</li>\n<li>Write in your own words: why \"no refund over the limit\" must be a HOOK, not a prompt line.</li>\n<li>List the 3 handoff fields from memory: customer ID, root cause, recommended action.</li>\n<li>Note the two hook jobs from this unit: block a tool (PreToolUse) and clean output (PostToolUse).</li>\n<li>Write one line on orchestrator-workers vs parallelization — who picks the subtasks, and when.</li>\n</ul>"},
t2:{title:"Research multi-agent system",html:"<h3>🎯 What this lesson teaches</h3>\n<p>This unit builds a <b>research multi-agent system</b>. One boss agent splits a big question, hands pieces to worker agents, then collects a report.</p>\n<p>The pattern is: <b>search → analyze → synthesize → report</b>.</p>\n<p>You will learn the 4 moves — decompose, pass context, gather, and attach provenance — and the one place it usually breaks.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Research multi-agent system</b> — a <b>coordinator</b> (also called the <b>lead agent</b> or <b>orchestrator</b>) plans the work, and several <b>subagents</b> (workers) do the actual searching. The lead agent never searches the web itself. It is the editor, not the reporter.<br>🧪 <i>Example:</i> Question: \"Compare 5 companies' AI strategy.\" The lead agent gives each subagent one company to research, then stitches the 5 reports into one answer.<br>⚠️ <i>Trap:</i> thinking the boss does the searching. No — the boss <b>plans, delegates, and synthesizes</b>. The workers search.</div>\n\n<div class=\"idea\"><b>Decompose the question</b> — the coordinator breaks one big question into smaller <b>subtopics</b>, one per worker. Give each worker a <b>distinct</b> slice so two workers don't research the same thing.<br>🧪 <i>Example:</i> \"Research the EV market\" splits into: one worker on batteries, one on charging stations, one on car makers — not three workers all googling \"EV market.\"<br>⚠️ <i>Trap:</i> a fuzzy, vague task like \"research semiconductors\" makes two workers wander into the SAME search. Duplicated work wastes tokens.</div>\n\n<div class=\"idea\"><b>Orchestrator decides the subtasks (not a fixed list)</b> — this is what makes it more than plain <b>parallelization</b>. Parallelization runs <b>known, pre-planned</b> subtasks side by side. The orchestrator <b>decides the subtasks at runtime</b>, based on the actual input.<br>🧪 <i>Example:</i> parallelization = a fixed menu with 3 cooks. Orchestrator-workers = a head chef who invents the dishes for tonight's order, then hands them out.<br>⚠️ <i>Trap:</i> calling orchestrator-workers \"advanced parallelization.\" It is a DIFFERENT pattern — the subtasks are decided per input, not fixed ahead of time.</div>\n\n<div class=\"idea\"><b>Send each worker context (explicit context passing)</b> — a subagent runs in its <b>own fresh context window</b>. It does NOT automatically know what the coordinator knows. You must pass the needed context <b>inside its prompt</b>.<br>🧪 <i>Example:</i> tell the worker its objective, the output format, which tools to use, and its boundaries — like a mini-contract, not a hint.<br>⚠️ <i>Trap:</i> this is where the system usually BREAKS — a worker can't use context the coordinator never passed it. Nothing is inherited; the prompt is the only channel.</div>\n\n<div class=\"idea\"><b>Gather and dedupe</b> — after workers report back, the coordinator collects the findings and removes duplicates. If there are gaps, it can send a <b>second round</b> of workers to fill them. Decomposition can even <b>adapt mid-research</b> as new findings come in.<br>🧪 <i>Example:</i> two workers both found \"the 30-day return policy\" — the coordinator keeps it once. It notices no one covered pricing, so it sends one more worker for that.<br>⚠️ <i>Trap:</i> thinking the plan is fixed at the start. A good coordinator can re-delegate to fill synthesis gaps.</div>\n\n<div class=\"idea\"><b>Provenance on every finding</b> — every finding must carry where it came from. A <b>finding = claim + source + date + confidence</b>. This lets a human audit the answer later.<br>🧪 <i>Example:</i> not just \"returns allowed for 30 days\" but \"returns allowed for 30 days — source: refund-policy.md, date: 2026-06-01, confidence: high.\"<br>⚠️ <i>Trap:</i> a claim with no source sounds confident but can't be verified. Attach source, date, and confidence to <b>every</b> finding.</div>\n\n<div class=\"idea\"><b>When NOT to use multi-agent</b> — skip it when subtasks are <b>tightly coupled</b> — when each step depends on the last and they need to share live, changing details. Subagents work apart and only report at the end, so tangled steps trip over each other.<br>🧪 <i>Example:</i> \"research 5 unrelated companies\" splits great. \"Negotiate a deal where each step depends on the last agreement\" does NOT — keep that in one loop.<br>⚠️ <i>Trap:</i> \"too big for one context window\" is a reason FOR multi-agent, not against — each subagent brings its own fresh window. Multi-agent also costs ~15× the tokens of a plain chat, so it's only worth it for high-value tasks.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>coordinator / lead agent / orchestrator</b> — the boss agent that plans, delegates, and synthesizes; it does not search itself.</li>\n<li><b>subagent / worker</b> — an agent that does one slice of the research and reports back.</li>\n<li><b>decompose</b> — split one big question into smaller distinct subtopics.</li>\n<li><b>explicit context passing</b> — putting needed info into a worker's prompt, because it inherits nothing.</li>\n<li><b>parallelization</b> — running KNOWN subtasks at the same time (subtasks are fixed, not decided at runtime).</li>\n<li><b>synthesize</b> — the coordinator stitching worker reports into one answer.</li>\n<li><b>provenance</b> — tracking the source of every claim (source + date + confidence).</li>\n<li><b>tightly-coupled task</b> — a task whose steps depend on each other and need shared live context; a poor fit for multi-agent.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>You should know that <b>orchestrator-workers differs from plain parallelization</b> because the orchestrator <b>decides the subtasks dynamically</b> (per input), while parallelization runs pre-defined subtasks.</li>\n<li>You should know <b>why you pass a subagent explicit context</b>: it runs in a fresh context window and inherits nothing automatically — so file paths, errors, and decisions must go in its prompt.</li>\n<li>You should know <b>when multi-agent is a POOR fit</b>: tightly-coupled tasks that need shared context mid-flight. (Big parallelizable research and tasks bigger than one context window are GOOD fits.)</li>\n<li>You should know what <b>provenance</b> means: tracking where each claim/fact came from — for trust and auditing.</li>\n<li>You should be able to describe the coordinator's job: <b>decompose → send context → gather + dedupe → synthesize</b>, with the lead agent planning and workers searching.</li>\n<li>You should know a good <b>finding</b> is a claim plus its source, date, and confidence — not a bare claim.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write the 4 moves in your own words: decompose, pass context, gather/dedupe, attach provenance.</li>\n<li>Write one line on WHY orchestrator-workers is not just \"parallelization\" (who decides the subtasks?).</li>\n<li>Write the sentence: \"A subagent inherits ______ automatically, so I must ______.\" Fill the blanks.</li>\n<li>Write your own tiny example of a good finding: claim + source + date + confidence.</li>\n<li>Write one case where you would NOT use multi-agent, and why.</li>\n</ul>"},
t3:{title:"PR-review / CI pipeline",html:"<h3>🎯 What this lesson teaches</h3>\n<p>This lesson builds a <b>code-review pipeline</b> that runs inside <b>CI</b> (the checks that run when you open a pull request).</p>\n<p>You will learn what should block a merge versus what can wait, how to review in two passes, how to run Claude non-interactively, and how to stop the review from crying wolf.</p>\n<p>These ideas are tested by the reliability quiz. One tiny example each, then how the quiz asks them.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>CI pipeline</b> — the set of automatic checks that run when someone opens a <b>PR</b> (a pull request: a proposed code change). The review is one of those checks.<br>🧪 <i>Example:</i> you open a PR. A robot review reads your changed files and posts comments before a human looks.<br>⚠️ <i>Trap:</i> the review is a step in the pipeline, not the whole thing. Tests and linters run too.</div>\n\n<div class=\"idea\"><b>Real-time (blocking) vs batch (overnight)</b> — pick <b>real-time</b> for checks that must pass before a merge; pick <b>batch</b> for work that can wait, because batch is cheaper.<br>🧪 <i>Example:</i> a fast \"did you break the build?\" check blocks the merge (real-time). A slow \"scan the whole repo for smells\" report runs overnight (batch).<br>⚠️ <i>Trap:</i> a <b>blocking pre-merge gate</b>, a live checkout step, and an emergency escalation are all real-time. Only non-urgent work that can tolerate delay is a good fit for batch.</div>\n\n<div class=\"idea\"><b>claude -p with JSON output</b> — <code>claude -p</code> runs Claude <b>headless</b> (no chat window, just in and out) so a script can call it. Ask for <b>JSON output</b> so the pipeline can read the result by field.<br>🧪 <i>Example:</i> CI runs <code>claude -p \"review this file\"</code> and gets back <code>{\"issues\":[...], \"verdict\":\"pass\"}</code> — the script reads <code>verdict</code> and decides to block or allow.<br>⚠️ <i>Trap:</i> free-text is hard for a script to parse. Structured Outputs (constrained decoding) is the reliable way to guarantee the JSON is valid — not \"ask nicely\" or \"retry until it parses.\"</div>\n\n<div class=\"idea\"><b>File-pass then integration-pass</b> — first review each file on its own for local issues; THEN do a second pass across files for how they fit together.<br>🧪 <i>Example:</i> pass 1 finds a typo in <code>auth.js</code>. Pass 2 notices <code>auth.js</code> and <code>login.js</code> now expect different argument orders.<br>⚠️ <i>Trap:</i> a per-file review alone misses cross-file bugs. You need BOTH passes, in that order.</div>\n\n<div class=\"idea\"><b>False-positive reduction</b> — a <b>false positive</b> is the review flagging something that is not actually a problem. Cut them with clear, explicit criteria in the prompt.<br>🧪 <i>Example:</i> instead of \"find problems,\" you say \"flag only: unhandled errors, missing input checks, hard-coded secrets.\" Fewer noisy comments.<br>⚠️ <i>Trap:</i> vague prompts create noise, and noise trains people to ignore the review. Clear criteria = fewer false positives.</div>\n\n<div class=\"idea\"><b>Re-running a review</b> — on the next push, pass the review its <b>prior findings</b> so it flags only NEW or still-unaddressed issues.<br>🧪 <i>Example:</i> round 1 found 4 issues; the author fixed 3. Round 2 gets the old list and reports just the 1 leftover plus anything new.<br>⚠️ <i>Trap:</i> without the prior findings, every re-run re-posts the same old comments — more noise, more false positives.</div>\n\n<div class=\"idea\"><b>Lost-in-the-middle</b> — when the review reads a very long context, important info buried in the MIDDLE gets overlooked. The model reads the start and end best.<br>🧪 <i>Example:</i> a huge diff pasted in one blob; a real bug sitting halfway down is skipped.<br>⚠️ <i>Trap:</i> this is a RECALL weakness, not a crash and not broken JSON. The fix is prune or summarize — and this is exactly why you review file-by-file instead of one giant blob.</div>\n\n<div class=\"idea\"><b>Provenance</b> — tracking where each review comment came from: which file and which line.<br>🧪 <i>Example:</i> \"unhandled error → source: payment.js, line 42.\" A human can jump straight to it and check.<br>⚠️ <i>Trap:</i> provenance is source-tracking, NOT a model's name, a retry policy, or a CSS style. A comment with no location can't be trusted or verified.</div>\n\n<div class=\"idea\"><b>Human-in-the-loop review</b> — a person makes the final call on cases that are UNCERTAIN, RISKY, or POLICY-SENSITIVE. The robot advises; the human decides the tricky ones.<br>🧪 <i>Example:</i> the review auto-approves a tiny typo fix, but a change to the payment code is routed to a senior engineer.<br>⚠️ <i>Trap:</i> it is NOT there to slow everything down or replace automation. Only the shaky cases go to a person.</div>\n\n<div class=\"idea\"><b>Verify with rules/code, not vibes</b> — the MOST robust way to check the work is a rule or code check: tests, types, linters. A person eyeballing it (visual) is weaker. Asking a model to judge (LLM-judge) is the LEAST robust.<br>🧪 <i>Example:</i> instead of trusting the review's \"looks fine,\" the pipeline also runs the test suite and the linter — hard, repeatable checks.<br>⚠️ <i>Trap:</i> \"ask the model if it's sure,\" \"trust it,\" and \"longer prompts\" are all weak. Code checks give hard, repeatable feedback.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>CI</b> — automatic checks that run when code is proposed</li>\n<li><b>PR / pull request</b> — a proposed code change waiting to be merged</li>\n<li><b>blocking check</b> — must pass before merge (real-time)</li>\n<li><b>batch</b> — cheaper processing for work that can wait (e.g. an overnight report)</li>\n<li><b>real-time</b> — for live or blocking steps</li>\n<li><b>claude -p</b> — run Claude headless/non-interactive so scripts and CI can call it</li>\n<li><b>JSON output</b> — structured result a script can read by field</li>\n<li><b>Structured Outputs</b> — constrained decoding that guarantees valid JSON</li>\n<li><b>file-pass</b> — review each file on its own for local issues</li>\n<li><b>integration-pass</b> — a second pass across files for how they fit together</li>\n<li><b>false positive</b> — flagging something that isn't actually a problem</li>\n<li><b>lost-in-the-middle</b> — key info in the middle of a long context gets overlooked</li>\n<li><b>provenance</b> — tracking the source (file + line) of each finding</li>\n<li><b>human-in-the-loop</b> — a person decides uncertain/risky/policy cases</li>\n<li><b>verification ladder</b> — code &gt; visual &gt; LLM-judge, most to least robust</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Know when <b>batch</b> fits: work that can wait (delayed results OK, cheaper). Reject the live checkout step, the emergency escalation, and the blocking pre-merge gate — those are real-time.</li>\n<li>Define <b>lost-in-the-middle</b>: important info buried in a long context gets overlooked. Not a crash, not broken JSON, not a tool failure. The fix is prune/summarize (and review per file).</li>\n<li>Define <b>provenance</b>: tracking where each claim/finding came from. Not a model name, retry policy, or CSS style.</li>\n<li>Know <b>human-in-the-loop</b> is for routing uncertain, risky, or policy-sensitive cases to a person — not for slowing things down or replacing automation.</li>\n<li>Name the MOST robust check: rule/code-based checks (tests, types, linters), above visual, well above LLM-judge. \"Ask the model,\" \"trust it,\" and \"longer prompts\" are weak.</li>\n<li>Bonus for this unit: <b>claude -p</b> is for non-interactive CLI use in automation (CI); the most reliable schema-valid JSON comes from Structured Outputs (constrained decoding).</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write one line: real-time = blocking pre-merge gate; batch = overnight report that can wait / cheaper.</li>\n<li>Draw the two passes: box 1 \"each file alone\" → box 2 \"across files.\" Note one bug each pass catches.</li>\n<li>Note why you review file-by-file: it avoids lost-in-the-middle from one giant diff.</li>\n<li>Jot how to cut false positives: explicit criteria in the prompt + pass prior findings on re-runs.</li>\n<li>Copy the verification ladder (code &gt; visual &gt; LLM-judge) and one reason code wins.</li>\n</ul>"},
p1:{title:"The 6 exam scenarios",html:"<h3>🎯 What this lesson teaches</h3>\n<p>The exam gives you <b>scenarios</b> — little stories about a real system with a problem.</p>\n<p>Your job for each one: read it, pick the right approach, and say <i>why</i>.</p>\n<p>This lesson gives you a repeatable method so you never have to guess.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>The 4-step scenario method</b> — every scenario answer follows the same four moves. (1) Read the stem carefully. (2) Pick the box: is this a <b>prompt</b>, a <b>tool</b>, an <b>agent loop</b>, or a <b>hook</b>? (3) Name the tradeoff you are making (cost, speed, or safety). (4) Add the reliability angle — what guardrail keeps it safe.<br>🧪 <i>Example:</i> \"Refunds over $500 must always escalate.\" Box = a hook. Tradeoff = a tiny bit slower for guaranteed safety. Reliability = the rule holds every time, not just when the model feels like it.<br>⚠️ <i>Trap:</i> jumping straight to the fanciest option (a full multi-agent system) when a single prompt would do. Simple first.</div>\n\n<div class=\"idea\"><b>Read the stem carefully</b> — the <b>stem</b> is the question's story. The answer is usually decided by one or two key words hiding in it. Underline them in your head.<br>🧪 <i>Example:</i> the word \"always\" (as in \"always escalate\") means a hard rule, which points to a hook, not a prompt.<br>⚠️ <i>Trap:</i> answering the topic instead of the question. A stem about tools might really be testing \"read-only, so use a resource\" — the key word was \"read\".</div>\n\n<div class=\"idea\"><b>Whole-system tradeoff thinking</b> — a real system trades three things against each other: <b>cost</b> (money/tokens), <b>latency</b> (how long it takes), and <b>safety</b> (how sure you are it is right). You rarely get all three. Good answers say which one they are trading.<br>🧪 <i>Example:</i> running a review 3 times and voting is safer but costs 3x. A single call is cheap but less sure.<br>⚠️ <i>Trap:</i> thinking one answer is \"just better.\" On the exam, the best answer <i>fits the scenario's priority</i> — a live checkout wants speed; a legal decision wants safety.</div>\n\n<div class=\"idea\"><b>Eliminate the distractors</b> — a <b>distractor</b> is a wrong answer built to look right. Most scenario questions have one classic trap. If you can name why three options are wrong, the fourth is your answer.<br>🧪 <i>Example:</i> options that \"invent the missing fact\", \"retry forever\", or \"lower the font size\" are all obvious traps — so \"mark it missing / send to a human\" wins.<br>⚠️ <i>Trap:</i> picking the option that <i>sounds</i> most technical. Fancy wording is often the bait.</div>\n\n<div class=\"idea\"><b>Pick the right box: prompt vs tool vs agent vs hook</b> — four common \"boxes\" answer most scenarios. A <b>prompt</b> = instructions (good, but the model <i>can</i> ignore it). A <b>tool</b> = one action the model calls. An <b>agent loop</b> = think-act-repeat for open-ended jobs. A <b>hook</b> = code that runs no matter what (a hard gate).<br>🧪 <i>Example:</i> \"look up an order\" = a tool. \"Keep working until the task is done\" = an agent loop. \"Block this every time\" = a hook.<br>⚠️ <i>Trap:</i> using a prompt for a must-hold safety rule. Prompts are suggestions; hooks are guarantees. Deterministic rules go in code.</div>\n\n<div class=\"idea\"><b>Add the reliability angle</b> — nearly every scenario has a hidden \"how do we keep it safe?\" part. The go-to guardrails: send <b>uncertain / risky / policy-sensitive</b> cases to a human, keep <b>provenance</b> (track the source of each fact), and verify with code before trusting the model.<br>🧪 <i>Example:</i> a $20 refund the agent handles alone; a $600 one goes to a human because policy caps solo approvals.<br>⚠️ <i>Trap:</i> escalating on <b>sentiment</b> (an angry customer) instead of <b>complexity/risk</b> (a big or tricky case). Escalate on how hard/risky it is, not on mood.</div>\n\n<div class=\"idea\"><b>Simple before complex</b> — the exam rewards the <i>smallest</i> thing that works. A single good prompt with retrieval and a few examples solves a lot. Agents and multi-agent systems cost more money and time — only reach for them when the task is truly open-ended.<br>🧪 <i>Example:</i> \"classify this ticket\" = one prompt. \"Fix this whole codebase\" = maybe an agent.<br>⚠️ <i>Trap:</i> grabbing multi-agent for a job one loop could do, or grabbing a framework before you understand what it hides (glass box first, magic box later).</div>\n\n<div class=\"idea\"><b>The stop signal is stop_reason, not the text</b> — in an agent loop, you decide whether to keep going by reading <b>stop_reason</b> (tool_use = keep going, end_turn = done). You never read the model's words to guess if it is finished, and you never rely on a fixed \"stop after 5 turns\" as your main stop.<br>🧪 <i>Example:</i> stop_reason says tool_use → run the tool, loop again. It says end_turn → stop.<br>⚠️ <i>Trap:</i> \"parse the text for the word 'done'\" or \"cap it at 5 iterations\" — both are classic wrong answers. A cap is only a safety backstop, not the real stop.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>scenario</b> — a short story about a real system with a decision to make</li>\n<li><b>stem</b> — the wording of the question; the key clue lives here</li>\n<li><b>distractor</b> — a wrong answer written to look tempting</li>\n<li><b>tradeoff</b> — giving up one thing (cost, speed, safety) to gain another</li>\n<li><b>latency</b> — how long something takes to respond</li>\n<li><b>prompt</b> — instructions to the model; it can ignore them</li>\n<li><b>tool</b> — one action you let the model call</li>\n<li><b>agent loop</b> — think → act → read result → repeat → finish</li>\n<li><b>hook</b> — code that runs deterministically as a hard gate</li>\n<li><b>stop_reason</b> — the real signal that runs an agent loop (tool_use vs end_turn)</li>\n<li><b>provenance</b> — tracking where each fact came from (source, date)</li>\n<li><b>human-in-the-loop</b> — routing uncertain/risky/policy cases to a person</li>\n<li><b>batch</b> — work done later, in a group, when results can wait</li>\n<li><b>real-time</b> — work done now, for live or blocking steps</li>\n<li><b>resource (MCP)</b> — read-only context; use it when Claude only needs to READ</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>Given a scenario, know how to <b>read the stem</b> and spot the one or two key words that decide the answer.</li>\n<li>Be able to <b>pick the box</b>: prompt, tool, agent loop, or hook — and say why it fits.</li>\n<li>Know that a rule that <b>must always hold</b> (safety-critical) goes in code/a hook, not just a prompt (community-reported favorite trap).</li>\n<li>Name the <b>tradeoff</b> in an answer: is it trading cost, latency, or safety — and does that match the scenario's priority?</li>\n<li>Add the <b>reliability angle</b>: route uncertain/risky/policy-sensitive cases to a human; escalate on <b>complexity/risk</b>, never on sentiment.</li>\n<li>Know that an agent loop stops on <b>stop_reason</b> (tool_use = continue, end_turn = done) — not by reading the text and not by a fixed iteration cap.</li>\n<li>Choose <b>simple over complex</b>: one optimized prompt (+ retrieval + examples) often beats an agent; multi-agent only for truly open-ended jobs.</li>\n<li>Recognize the <b>read-only</b> clue: if Claude only needs to READ, pick an MCP <b>resource</b>, not a tool.</li>\n<li>Handle missing data right: if a required fact is <b>not in the source</b>, mark it missing or send to a human — never invent it.</li>\n<li>Handle a <b>formatting</b> failure (bad JSON) by retrying <i>with the specific error included</i> — different from a real missing fact, which goes to a human.</li>\n<li>Pick <b>batch</b> when results can wait; pick <b>real-time</b> for live or blocking steps.</li>\n<li>Spot the <b>lost-in-the-middle / overflow</b> risk in long, verbose conversations: key info gets buried or pushed out of context.</li>\n<li>Know the <b>multi-pass review</b> pattern: file-by-file local pass, then a cross-file integration pass — not one giant pass.</li>\n<li>Eliminate distractors: name why the other three options are wrong, and the last one is your answer.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write the 4-step method in your own words: read → pick the box → name the tradeoff → add the guardrail.</li>\n<li>Make your own \"box picker\": one line each for when to use a prompt, a tool, an agent loop, and a hook.</li>\n<li>List the 3 tradeoffs (cost, latency, safety) and one scenario where each one wins.</li>\n<li>Write the 2 escalation words that matter — <b>risk</b> and <b>complexity</b> — and cross out \"sentiment\".</li>\n<li>Note your top 3 distractor traps to watch for (e.g. parse-the-text-to-stop, prompt-a-safety-rule, invent-the-missing-fact).</li>\n</ul>"},
p2:{title:"Your capstone build",html:"<h3>🎯 What this lesson teaches</h3>\n<p>This is your <b>capstone</b>: build one real thing, end to end.</p>\n<p>You pick a small system, draw its plan, build a tiny working version, add one safety check, and explain it in 60 seconds.</p>\n<p>There is no quiz here. The \"test\" is the project rubric — can you show it, defend it, and say what can fail?</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Small + real</b> — pick something tiny but true, not big and fake. One narrow job you can finish and explain beats a giant idea you can't.<br>🧪 <i>Example:</i> a study-tracker that saves one note and grades one answer — not \"an AI that runs my whole life.\"<br>⚠️ <i>Trap:</i> scoping too big. If you can't build a tiny version this week, it's too big. Cut it down.</div>\n\n<div class=\"idea\"><b>The blueprint</b> — a simple map of your system before you build. It shows the parts and how they connect: input, the work, the tools/data, and the final answer.<br>🧪 <i>Example:</i> user asks → Claude thinks → calls a lookup_order tool → reads the result → replies.<br>⚠️ <i>Trap:</i> building with no plan. If you can't draw it, you can't explain it — and the rubric grades whether you can point to each part.</div>\n\n<div class=\"idea\"><b>Tiny working version</b> — the smallest thing that actually runs. Not the whole dream, just one path working end to end.<br>🧪 <i>Example:</i> one prompt that takes a receipt and returns the total as JSON. That's enough to start.<br>⚠️ <i>Trap:</i> thinking you must build the full system. A tiny version that runs teaches more than a big design that doesn't.</div>\n\n<div class=\"idea\"><b>One guardrail</b> — a safety check that stops a bad action. The exam's key point: put critical rules in <b>code (a hook or app gate)</b>, not just in the prompt. A prompt is a request the model can ignore; code cannot be ignored.<br>🧪 <i>Example:</i> \"any refund over $500 is blocked and sent to a human.\" Your code enforces this, so the model can never overspend — even if it wants to.<br>⚠️ <i>Trap:</i> \"I'll just tell Claude in the prompt not to do it.\" Prompt-only rules are not enough when the rule is safety-critical. Deterministic compliance needs code.</div>\n\n<div class=\"idea\"><b>Human-in-the-loop</b> — a person checks the work before it counts, whenever a case is <b>uncertain, risky, or policy-sensitive</b>. This is a common guardrail choice.<br>🧪 <i>Example:</i> the agent approves a $20 refund alone, but a $600 one goes to a human because policy caps solo approvals at $500.<br>⚠️ <i>Trap:</i> letting the model's own \"I'm confident\" be the ONLY thing that decides if a human looks. Confidence can guide where to look first, but must not be the only gate.</div>\n\n<div class=\"idea\"><b>Validation</b> — checking an output is correct before you trust it. Rule/code checks are the most robust, then visual, then an LLM-judge (least robust). <b>Structured Outputs</b> use constrained decoding to guarantee the JSON is valid.<br>🧪 <i>Example:</i> after Claude returns JSON, your code checks the total is a number and the date is real before saving it.<br>⚠️ <i>Trap:</i> trusting an \"LLM says it's fine\" as your only check. A code test beats a model's opinion.</div>\n\n<div class=\"idea\"><b>Explain it in 60 seconds</b> — say out loud what your system does, what can fail, how a failure is caught, and what happens next. This is the real capstone skill: architect, not just recall.<br>🧪 <i>Example:</i> \"User sends a refund. My code checks the limit. Over $500 it escalates to a human. That's my guardrail.\"<br>⚠️ <i>Trap:</i> saying \"Claude just handles it.\" You must name the parts and the failure paths, not hand-wave.</div>\n\n<div class=\"idea\"><b>The rubric ladder</b> — how the project is graded, low to high: <b>Beginner</b> (name the parts), <b>Developing</b> (explain the normal path), <b>Exam-ready</b> (normal path + failures + why your design beats alternatives), <b>Strong</b> (teach it visually and adapt it to a new scenario).<br>🧪 <i>Example:</i> naming \"tool, agent, guardrail\" is Beginner; explaining why a hook beats a prompt is Exam-ready.<br>⚠️ <i>Trap:</i> stopping at \"it works.\" Exam-ready means you also cover what fails and why your choice is better than the alternative.</div>\n\n<div class=\"idea\"><b>Combine the pieces (final capstone)</b> — the top project stitches everything into one diagram: input, real-time route, batch route, tool layer, MCP resources, structured output, validation, human review, context pruning, shared scratchpad, final response.<br>🧪 <i>Example:</i> your blueprint has a box for each of these, and you can point to every box and say what can fail there.<br>⚠️ <i>Trap:</i> a pretty diagram you can't defend. The \"Done when\" is: point to every part and explain what fails, how it's detected, and what happens next.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>capstone</b> — one end-to-end build that proves you can architect a whole system.</li>\n<li><b>scope</b> — how big or small the job is; keep it small so you can finish it.</li>\n<li><b>blueprint</b> — a simple diagram of your system's parts and how they connect.</li>\n<li><b>guardrail</b> — a safety check (usually in code) that blocks a bad action.</li>\n<li><b>hook</b> — code that runs at a set point to enforce a hard rule the model can't skip.</li>\n<li><b>app gate</b> — a check in your own app code that blocks or allows an action.</li>\n<li><b>validation</b> — checking an output is correct before trusting it.</li>\n<li><b>Structured Outputs</b> — a mode that guarantees valid JSON via constrained decoding.</li>\n<li><b>human-in-the-loop</b> — a person reviews the work when a case is uncertain, risky, or policy-sensitive.</li>\n<li><b>deterministic compliance</b> — a rule that must be obeyed every time; needs code, not a prompt.</li>\n<li><b>rubric</b> — the grading scale: Beginner, Developing, Exam-ready, Strong.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<p class=\"hint\">There is no quiz for this unit. Instead your project is graded by the rubric. Here is what each graded item expects you to be able to do:</p>\n<ul>\n<li>Pick a project that is <b>small and real</b>, and say why a huge or fake idea is worse.</li>\n<li>Draw a <b>blueprint</b> and point to each part: input, the work, tools/data, final answer.</li>\n<li>Build a <b>tiny working version</b> — the smallest path that runs end to end.</li>\n<li>Add <b>one guardrail</b> and explain WHY it lives in code (hook/app gate), not just the prompt.</li>\n<li>Know that <b>prompt-only rules are not enough</b> for safety-critical actions — deterministic compliance needs code.</li>\n<li>Choose <b>human review</b> for cases that are uncertain, risky, or policy-sensitive — and know confidence must not be the only gate.</li>\n<li>Name a <b>validation</b> step, and rank checks: code &gt; visual &gt; LLM-judge; know Structured Outputs guarantee valid JSON.</li>\n<li>Explain the whole thing in 60 seconds, naming <b>what can fail, how it's caught, and what happens next</b>.</li>\n<li>Know the <b>rubric ladder</b> (Beginner → Developing → Exam-ready → Strong) and what moves you up: adding failures and tradeoffs, not just \"it works.\"</li>\n<li>For the <b>final capstone</b>, combine the parts (real-time + batch routes, tools, MCP resources, structured output, validation, human review, context pruning, scratchpad) into one defendable diagram.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write down your one small, real project idea in a single sentence — and why it's small enough to finish.</li>\n<li>Sketch your blueprint boxes and arrows in your own words: input → work → tools → answer.</li>\n<li>Write your one guardrail and finish this line: \"This rule is in code, not the prompt, because…\"</li>\n<li>List one thing that can fail, how you'd catch it, and what happens next (retry? escalate to a human?).</li>\n<li>Write your 60-second explanation as if teaching a friend — then mark which rubric rung you're on.</li>\n</ul>"},
px:{title:"Anti-patterns & decision frameworks",html:"<h3>🎯 What this lesson teaches</h3>\n<p>The CCA-F exam is mostly scenario multiple-choice. In each one, a few options are <b>classic wrong answers</b> — traps.</p>\n<p>This lesson teaches the 10 famous traps, so you can spot them fast. Then it teaches how to pick between two good-looking options (A vs B).</p>\n<p>Learn to name the trap and you have half the answer already.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>Anti-pattern</b> — a way of doing something that looks fine but is known to go wrong. On the exam it is the tempting wrong option.<br>🧪 <i>Example:</i> \"stop the loop when the text says 'done'\" sounds smart but is fragile — it is a trap.<br>⚠️ <i>Trap:</i> do not pick an option just because it sounds active or clever. Ask \"is this the known-bad way?\" first.</div>\n\n<div class=\"idea\"><b>Trap 1 — parsing text to stop a loop</b> — deciding an agent is finished by reading its words. Instead, check the real signal: <b>stop_reason</b>.<br>🧪 <i>Example:</i> the agent writes \"all done!\" but still wanted to call a tool. Reading the text stops it too early. Checking <code>stop_reason</code> (tool_use vs end_turn) does not.<br>⚠️ <i>Trap:</i> \"look for the word done / finished in the output\" is the wrong answer. Words are not a control signal.</div>\n\n<div class=\"idea\"><b>Trap 2 — iteration caps as the main stop</b> — using only a fixed number (\"stop after 10 loops\") to end an agent. A cap is a safety net, not the real brain.<br>🧪 <i>Example:</i> a cap of 10 stops a 12-step job too early, and lets a 2-step job spin 8 wasted times. It never checks the actual task state.<br>⚠️ <i>Trap:</i> \"just cap the iterations\" as the PRIMARY stop is weak. Use <code>stop_reason</code> to decide; keep the cap only as a backstop against runaway loops.</div>\n\n<div class=\"idea\"><b>Trap 3 — prompt-enforcing critical rules</b> — putting a must-never-break rule only in the prompt. The model can still slip. Hard rules belong in <b>code / hooks</b>.<br>🧪 <i>Example:</i> \"never refund over $500\" typed in the prompt can be ignored. A code check (a hook) that blocks the action always holds.<br>⚠️ <i>Trap:</i> when the exam says \"deterministic compliance\" or \"safety-critical,\" pick code/hooks — not \"add it to the prompt.\"</div>\n\n<div class=\"idea\"><b>Trap 4 — self-reported confidence as the escalation gate</b> — letting the model's own \"I'm 90% sure\" be the ONLY thing that decides if a human looks.<br>🧪 <i>Example:</i> a wrong answer said with high confidence never reaches a human. Confidence should guide WHERE humans look first, not IF they look at all.<br>⚠️ <i>Trap:</i> \"only escalate when the model says it is unsure\" is a trap. A model can be confidently wrong.</div>\n\n<div class=\"idea\"><b>Trap 5 — escalating on sentiment, not complexity</b> — handing off to a human because the customer <b>sounds</b> upset, instead of because the case is hard or risky.<br>🧪 <i>Example:</i> a calm customer with a legally tricky claim should escalate; an angry customer asking a simple question should not. Feelings are not risk.<br>⚠️ <i>Trap:</i> \"escalate when the customer sounds angry\" is the wrong answer. Escalate on COMPLEXITY / risk / policy.</div>\n\n<div class=\"idea\"><b>Trap 6 — generic errors that hide the cause</b> — returning a vague \"error\" or \"bad\" that tells the agent nothing. Good errors are structured.<br>🧪 <i>Example:</i> \"failed\" gives no path forward. \"timeout after 30s, tried endpoint A, no partial data, try endpoint B\" lets the agent recover.<br>⚠️ <i>Trap:</i> a one-word error is useless. Return a failure type, what was tried, partial results, and an alternative.</div>\n\n<div class=\"idea\"><b>Trap 7 — swallowing errors as success</b> — catching a failure and returning an empty result while calling it \"done.\" Silence hides the problem.<br>🧪 <i>Example:</i> a search fails, the tool returns an empty list, and the agent proudly says \"no matches found.\" It never knew anything broke.<br>⚠️ <i>Trap:</i> \"silently return empty and continue\" is a classic wrong answer. Also do not over-react: one failed record should not kill the whole batch.</div>\n\n<div class=\"idea\"><b>Trap 8 — too many tiny tools</b> — giving the agent ~18 near-identical thin tools. It picks wrong. A tight set of ~4–5 clear tools is better.<br>🧪 <i>Example:</i> 18 lookup tools with fuzzy names confuse the model. One well-described \"find_order\" beats five overlapping ones.<br>⚠️ <i>Trap:</i> \"add more tools\" is not automatically better. Split a generic tool only to make the CHOICE clearer — not to bloat the menu.</div>\n\n<div class=\"idea\"><b>Trap 9 — same-session self-review</b> — asking the model to review its own work in the same chat that produced it. It is biased toward its own answer.<br>🧪 <i>Example:</i> \"now check your code\" in the same session rubber-stamps the mistake. A fresh session (or code checks) catches more.<br>⚠️ <i>Trap:</i> reviewing in the same context is weak. Prefer a fresh pass, explicit review criteria, or code-based checks.</div>\n\n<div class=\"idea\"><b>Trap 10 — aggregate-only metrics</b> — reporting one overall score that hides which case TYPE is failing.<br>🧪 <i>Example:</i> \"94% accurate\" looks great, but hides that refunds fail 40% of the time. Break metrics down per type to see it.<br>⚠️ <i>Trap:</i> a single average can bury a broken category. Slice metrics by type / field so per-type failures show.</div>\n\n<div class=\"idea\"><b>Trap 11 — grabbing a framework before you understand it</b> — reaching for a big library first, so you never see the prompts, tools, and errors underneath.<br>🧪 <i>Example:</i> a framework hides the raw API call. When it breaks you cannot debug it. Start raw (glass box), then add a framework once you know what it hides.<br>⚠️ <i>Trap:</i> \"use the framework to start\" is not the safe default. Start with raw API calls; the framework is a magic box until you have opened it.</div>\n\n<div class=\"idea\"><b>Decision: hooks vs prompt</b> — a hook is code that always runs; a prompt is a request the model may ignore. Use hooks for hard, must-hold rules.<br>🧪 <i>Example:</i> \"block any command that deletes files\" → hook. \"prefer a friendly tone\" → prompt.<br>⚠️ <i>Trap:</i> anything safety-critical or \"deterministic\" goes to code/hooks, never prompt-only.</div>\n\n<div class=\"idea\"><b>Decision: plan mode vs direct</b> — plan first when a task is complex with several valid approaches; go direct when the step is small and obvious.<br>🧪 <i>Example:</i> a big refactor across many files → plan mode. Rename one variable → just do it.<br>⚠️ <i>Trap:</i> planning a one-word task wastes effort; diving straight into a messy multi-path task causes rework.</div>\n\n<div class=\"idea\"><b>Decision: sync vs batch</b> — real-time (sync) for live or blocking steps; batch for work that can wait, because batch is cheaper.<br>🧪 <i>Example:</i> a live checkout or a pre-merge gate must be real-time. An overnight report can be batch.<br>⚠️ <i>Trap:</i> emergency escalation and blocking gates are NOT batch fits. Batch = \"delayed results are OK.\"</div>\n\n<div class=\"idea\"><b>Decision: multi-pass vs single-pass review</b> — for code review, do a file-by-file local pass FIRST, then a cross-file integration pass.<br>🧪 <i>Example:</i> review each file for local bugs, then a second pass for how the files fit together. One giant pass misses the seams.<br>⚠️ <i>Trap:</i> \"one big pass over everything\" is the weaker answer. Two focused passes catch more.</div>\n\n<div class=\"idea\"><b>Decision: context is always explicit</b> — a subagent does NOT inherit the parent's memory. You must pass it what it needs.<br>🧪 <i>Example:</i> a subagent gets an objective, output format, tool guidance, and boundaries — plus the facts it needs. Vague tasks make two subagents duplicate work.<br>⚠️ <i>Trap:</i> \"the subagent already knows the context\" is false. Pass context on purpose, every time.</div>\n\n<div class=\"idea\"><b>Coordinator role (multi-agent)</b> — in a multi-agent system the lead/coordinator decomposes the work, routes context to subagents, aggregates results, and handles gaps.<br>🧪 <i>Example:</i> a research lead splits a question into pieces, hands each to a subagent, then stitches the findings into one answer. It never searches the web itself.<br>⚠️ <i>Trap:</i> the coordinator does NOT run every tool personally or hide sources. Its job is decompose → route → aggregate → fill gaps.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>anti-pattern</b> — a tempting way of doing something that is known to go wrong (the exam trap)</li>\n<li><b>stop_reason</b> — the real signal for whether an agent loop keeps going (tool_use) or is done (end_turn)</li>\n<li><b>iteration cap</b> — a fixed max number of loops; a backstop, not the primary stop</li>\n<li><b>hook</b> — code that always runs to enforce a hard rule (beats a prompt-only rule)</li>\n<li><b>deterministic compliance</b> — a rule that must hold every single time → use code, not prompt</li>\n<li><b>self-reported confidence</b> — the model's own \"how sure am I\"; must not be the only escalation gate</li>\n<li><b>sentiment</b> — how the customer feels; NOT a reason to escalate (use complexity/risk)</li>\n<li><b>structured error</b> — an error that carries type, what was tried, partial results, and alternatives</li>\n<li><b>swallowing errors</b> — hiding a failure and calling an empty result \"success\"</li>\n<li><b>tool bloat</b> — too many tiny overlapping tools that confuse tool choice (~18 vs ~4–5)</li>\n<li><b>same-session self-review</b> — a model reviewing its own work in the same chat; biased</li>\n<li><b>aggregate metrics</b> — one overall number that can hide a failing case type</li>\n<li><b>framework / glass box vs magic box</b> — start raw (glass box) so you see prompts/tools/errors before trusting a framework (magic box)</li>\n<li><b>coordinator / lead agent</b> — decomposes work, routes context, aggregates results, handles gaps</li>\n<li><b>explicit context</b> — you must pass a subagent what it needs; it does not inherit parent context</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>An agent loop should check <b>stop_reason</b> to know whether to keep going — NOT the color, paragraph count, filename, or any parsed text.</li>\n<li>Fixed <b>iteration caps</b> are a weak PRIMARY stop: they may stop too early or too late without understanding task state. Use them only as a backstop.</li>\n<li>Enforce a business rule <b>in code (hooks)</b>, not just the prompt, when <b>deterministic compliance</b> is required (safety-critical, must-hold rules).</li>\n<li>When JSON validation fails on formatting, <b>retry including the validation error in the prompt</b> — do not ignore it, delete the schema, or escalate to a human every time.</li>\n<li>When a required fact is missing from the source, <b>mark it missing or route to human review</b> — never invent it, never retry forever.</li>\n<li>Design your OWN tool error payload to carry an <b>error category (like \"timeout\") plus your own retry-me flag</b> — not \"bad,\" not a fake success, not nothing. (The built-in flag is is_error / isError; \"retry-me\" is a field YOU invent inside your payload.)</li>\n<li>Split one generic tool into smaller tools to <b>make tool choice clearer</b> — not to burn tokens or avoid descriptions. But avoid the opposite trap: too many tiny tools.</li>\n<li>Tool <b>descriptions</b> matter because the model uses them to pick the right tool — they do not replace validation or hide errors.</li>\n<li><b>Human-in-the-loop</b> review is for routing uncertain, risky, or policy-sensitive cases to a person — not to slow everything down or replace automation.</li>\n<li>Escalate on <b>complexity / risk</b>, not on how the customer sounds (sentiment).</li>\n<li>The main risk of long conversations with many verbose tool results: <b>important information gets buried or pushed out of context</b>.</li>\n<li><b>Batch</b> fits work that can tolerate delayed results — NOT a live checkout, a blocking pre-merge check, or an emergency escalation.</li>\n<li>Good code-review pattern: <b>file-by-file local review, then a cross-file integration review</b> — not one giant pass.</li>\n<li>A <b>coordinator</b> decomposes work, routes context, aggregates results, and handles gaps — it does not run every tool itself or hide sources.</li>\n<li>Subagent context must be <b>passed explicitly</b> — subagents do not inherit all parent context.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Make a \"trap list\": write all 10 traps as one short line each, in your own words (e.g. \"text-to-stop → use stop_reason\").</li>\n<li>Write the rule for hooks vs prompt: hard/safety rule → code/hook; soft preference → prompt.</li>\n<li>Jot the escalation rule: escalate on COMPLEXITY/risk, never on sentiment or self-confidence alone.</li>\n<li>Note the coordinator's four jobs: decompose, route context, aggregate, handle gaps — and \"subagents do not inherit context.\"</li>\n<li>Write your own A-vs-B cheat line for each decision: hooks/prompt · plan/direct · sync/batch · multi-pass/single-pass · raw/framework.</li>\n</ul>"},
a1:{title:"Core vocabulary (refresh)",html:"<h3>🎯 What this lesson teaches</h3>\n<p>This is the vocabulary lesson. It teaches the 8 base words that every other topic is built from.</p>\n<p>The words are: <b>API, JSON, schema, token, context window, prompt, tool, model</b>.</p>\n<p>You already know most of these. The goal here is to keep them warm and clear, so the harder units make sense.</p>\n\n<h3>💡 The big ideas</h3>\n\n<div class=\"idea\"><b>API</b> — two programs talking to each other. You send a request, you get a response back.<br>🧪 <i>Example:</i> your code asks the Claude API \"summarize this note\" and the API sends back the summary.<br>⚠️ <i>Trap:</i> an API key is a secret password. Never write it in your code, a README, or your logs. Keep it in an environment variable instead.</div>\n\n<div class=\"idea\"><b>JSON</b> — a simple text way to write data with labels. Curly braces hold name-and-value pairs.<br>🧪 <i>Example:</i> <code>{ \"name\": \"Dan\", \"score\": 20 }</code> — each label has a value.<br>⚠️ <i>Trap:</i> JSON is a text data format, NOT a programming language, a database, or an AI model. One missing comma or quote and it will not parse.</div>\n\n<div class=\"idea\"><b>Schema</b> — the shape rules for data. It says which fields exist and what type each one is.<br>🧪 <i>Example:</i> an invoice schema says <code>total</code> must be a number and <code>notes</code> can be text or empty.<br>⚠️ <i>Trap:</i> a schema is NOT the AI's memory, an API key, or a chat history. It only describes the expected shape and types of data. Too strict breaks on messy data; too loose lets junk through.</div>\n\n<div class=\"idea\"><b>Token</b> — a small chunk of text the model reads and counts. Roughly a word-piece, not a whole word.<br>🧪 <i>Example:</i> \"unbelievable\" might be split into a few tokens like \"un\", \"believ\", \"able\".<br>⚠️ <i>Trap:</i> tokens are how length is measured. The context window is measured in tokens, so long text uses up space fast.</div>\n\n<div class=\"idea\"><b>Context window</b> — the model's short-term memory. It is how much text the model can hold at one time.<br>🧪 <i>Example:</i> paste a huge document plus a long chat, and the earliest text falls out of view — the model can no longer use it.<br>⚠️ <i>Trap:</i> the context window does NOT set your screen size, store your API key, or make things cheaper when bigger. If info falls outside the window, the model simply cannot use it.</div>\n\n<div class=\"idea\"><b>Prompt</b> — the instruction you give the model. It shapes what you get back.<br>🧪 <i>Example:</i> \"Extract the invoice total as JSON. If it is missing, return null.\"<br>⚠️ <i>Trap:</i> a prompt alone is not enough for safety-critical rules. For rules that MUST always hold, use code or hooks — you learn that in later units.</div>\n\n<div class=\"idea\"><b>Tool</b> — an action you let the model call. It lets the model do something, not just chat.<br>🧪 <i>Example:</i> a <code>lookup_order</code> tool lets the model check a real order instead of guessing.<br>⚠️ <i>Trap:</i> a vague tool description makes the model pick the wrong tool or send bad inputs. Clear names and descriptions matter.</div>\n\n<div class=\"idea\"><b>Model</b> — the AI brain itself that reads your prompt and writes a reply. It is what the API talks to.<br>🧪 <i>Example:</i> Claude is a model; you send it a prompt through the API and it returns text.<br>⚠️ <i>Trap:</i> the model on its own only chats. It becomes an <b>agent</b> when you give it tools and a loop: think, use a tool, read the result, repeat, then finish.</div>\n\n<h3>📖 Words to know</h3>\n<ul>\n<li><b>API</b> — two programs talking; you send a request, you get a response.</li>\n<li><b>API key</b> — a secret password for the API; keep it in an environment variable, never in code or logs.</li>\n<li><b>JSON</b> — a text format for structured data, written as labeled name-value pairs.</li>\n<li><b>schema</b> — the rules for the shape of data: which fields exist and what type each is.</li>\n<li><b>token</b> — a small chunk of text the model reads; length is counted in tokens.</li>\n<li><b>context window</b> — the model's short-term memory; text beyond it cannot be used.</li>\n<li><b>prompt</b> — the instruction you give the model.</li>\n<li><b>tool</b> — an action you let the model call so it can do things, not just talk.</li>\n<li><b>model</b> — the AI brain that reads the prompt and writes the reply.</li>\n<li><b>agent</b> — a model plus tools plus a loop that keeps acting on results until the job is done.</li>\n<li><b>environment variable</b> — a value your computer holds outside your code, used to store secrets like keys.</li>\n</ul>\n\n<h3>🧭 How the quiz will test you</h3>\n<ul>\n<li>What a <b>schema</b> describes — expect you to know it means the expected shape and types of data (which fields, what type), NOT memory, a key, or chat history.</li>\n<li>Why the <b>context window</b> matters — know that anything beyond it cannot be used; the model \"forgets\" it. It does not set screen size or store keys.</li>\n<li>What <b>JSON</b> is — a text format for structured data, not a language, database, or model.</li>\n<li>What turns a chat model into an <b>agent</b> — calling tools in a loop and acting on the results. Not a bigger prompt, not just using JSON, not a faster API.</li>\n<li>Where an <b>API key</b> should live — secret, in an environment variable. Never in a README, hard-coded in the file, or printed to logs.</li>\n<li>Also be ready to name each term plainly: what a <b>token</b>, <b>prompt</b>, <b>tool</b>, and <b>model</b> each are, since these 8 words feed every later unit.</li>\n</ul>\n\n<h3>✏️ For your notes</h3>\n<ul>\n<li>Write the 8 words in a column, and next to each write a 5-word plain meaning in your own words.</li>\n<li>Draw the API as a counter: you ask, the kitchen hands it back. Label the \"key\" as a secret.</li>\n<li>Write one line: \"context window = desk that only fits so many papers — extras fall off.\"</li>\n<li>Write the agent formula: model + tools + LOOP (think → tool → read result → repeat → finish).</li>\n<li>Write your own trap-reminder: \"API key never goes in code, README, or logs — use an environment variable.\"</li>\n</ul>"},
};
